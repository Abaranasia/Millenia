# Shimmer Reverb — Implementation Plan

This is the phased, checklist-level build plan for Millenia's shimmer reverb DSP and plugin shell. It does not re-argue architecture — it assumes and builds on the decisions already made in:

- [`shimmer-reverb-concepts.md`](./shimmer-reverb-concepts.md) — conceptual guide, tank/shifter rationale, roadmap sketch
- [`../.claude/skills/juce-plugin-dev/references/shimmer-reverb-architecture.md`](../.claude/skills/juce-plugin-dev/references/shimmer-reverb-architecture.md) — JUCE class mapping, starting parameter values, pitfall→mitigation table, real-time safety rules
- [`shimmer-reverb-open-source-survey.md`](./shimmer-reverb-open-source-survey.md) — eight external reference implementations, used here only as inspiration/cross-checks, not as a reason to change topology
- [`reverb-design-research-notes.md`](./reverb-design-research-notes.md) — academic-paper bibliography and the Eno/Lanois shimmer origin story, background reading only

**Committed, non-negotiable for this plan**: Dattorro plate tank topology, dual-delay-line crossfade pitch shifter inserted at the tank's feedback junction, bipolar shift range −24..+24 semitones, hand-written `juce::dsp::DelayLine` + manual read/write (never `juce::dsp::ProcessorChain`) for anything inside the feedback loop.

**Explicitly out of scope**: the goniometer/Lissajous-curve stereo visualizer. That work is deliberately deferred to a later, separate plan and is not referenced or scheduled here.

## Current state

*Updated after Phase 0 and Phase 1 — see [`reverb-dev-journal.md`](./reverb-dev-journal.md) for the full account.*

Phase 0 is complete: `juce_dsp` is in `Millenia.jucer`'s `<MODULES>`/VS2026 `<MODULEPATHS>`, and `Source/DSP/` exists with `ShimmerReverbEngine`, `DattorroTank`, `PitchShifter`, `DCBlocker`, `SafetyLimiter` skeletons (all still empty stubs — Phase 2+ work). `pluginFormats="buildVST3,buildAU,buildStandalone"` and the stereo-in/stereo-out `BusesProperties` in `PluginProcessor`'s constructor have been reviewed and confirmed as this project's actual targets (VST3 + Standalone are what the VS2026 exporter builds; `buildAU` is declared but inert with no Xcode exporter configured, left in place in case one's added later) — no longer an unexamined default.

Phase 1 is fully complete: it built and validated `ScratchSchroederTank`, a mono Freeverb-style 6-comb + 2-allpass throwaway tank, confirming the DSP plumbing (delay lines, `prepareToPlay` sizing, real-time safety) and that sample-rate changes (44.1/48/96 kHz) don't crash or corrupt the tail. That tank has since been removed as part of closing Phase 2 (see below) — it was always scoped as temporary.

Phase 2 is complete: `Source/DSP/DattorroTank.h/.cpp` implements the committed Dattorro plate topology (4-stage input diffuser, two cross-feeding tank branches with one-pole leaky-integrator damping, figure-eight cross-feed, and the algorithm's real 7-tap output formula) and is the sole reverb path wired into `processBlock` — the Phase 1 `ScratchSchroederTank` scratch tank has been fully removed (files, wiring, tests, `.jucer` references). Confirmed sounding like a real plate reverb across two independent live Standalone sessions with different audio sources. Three real bugs were caught and fixed along the way (wrong allpass formula causing an audio blowup, damping ~600x too aggressive, a single dominant output tap making it sound like a delay instead of a reverb) — see Phase 2's section below for details. A JUCE-framework-only Standalone crash (audio device hot-swap race, unrelated to this project's DSP) was also found, root-caused, and documented rather than blocking the phase.

Phase 3 is complete: `Source/DSP/PitchShifter.h/.cpp` (hand-rolled dual-delay-line crossfade shifter) is implemented and wired into `Source/DSP/ShimmerReverbEngine.h/.cpp`, which composes the tank + shifter into the actual shimmer feedback loop and is `PluginProcessor`'s sole DSP dependency. The original hardcoded-feedback-gain approach (settled at `0.15f` after three tuning rounds) turned out to be structurally inaudible — it ran the shifter as a second, separate, much weaker feedback loop competing with `DattorroTank`'s own internal `decayGain`-scaled recirculation, rather than the single loop `shimmer-reverb-architecture.md`'s topology actually specifies. Fixed by restructuring so the shifter sits *inside* the tank's own recirculation path (`DattorroTank::peekFeedbackSignal()` + a new two-argument `processSample()` overload); `decayGain` is now the single knob controlling both sustain and shimmer amount, tuned from `0.6f` to `0.7f` (still inside the architecture doc's documented 0.6–0.85 range; `0.8f` was tried and rejected by the decay-to-silence test). All 9 unit tests pass, and a live ear pass confirmed the shimmer character audibly improved. See Phase 3's section below for the full history.

Phase 4 is closed, with three items explicitly carried forward rather than silently dropped (see Phase 4's section below for details): `decayGain`'s default (`0.7f`) is a working value, not yet a *final* one — that gets settled for real once Phase 5 exposes it as a parameter; the Definition of Done's three manual/instrumented checks (multi-minute max-setting stress test, a correlation-meter A/B, a DC-offset measurement at the actual plugin output) were never formally run, only approximated by automated tests and live ear passes; and the `DCBlocker` only covers the shimmer's external feedback path — `DattorroTank`'s own internal recirculation loop (`decayGain * feedbackFromB`) has no DC-drift test at all. `SafetyLimiter` (memoryless soft-knee, replacing the Phase 3 `tanh` stopgap), the `DCBlocker` that does exist, and stereo decorrelation (quadrature-offset pitch shifting, chosen over per-channel tank duplication) are all implemented and unit-tested; a live ear pass confirmed the result "sounds better."

`PluginEditor` is still the unmodified "Hello World!" template — GUI work starts at Phase 6.

**Still true from the original assessment:** no `AudioProcessorValueTreeState`, no real parameters yet (Phase 5).

## Phased plan

### Phase 0 — Project scaffolding

**Goal**: Make the project buildable as a DSP project (module + file skeleton) with zero behavior change, so every later phase is additive.

Tasks:
- [x] Add the `juce_dsp` module to `Millenia.jucer` (`<MODULES>` list and `<MODULEPATH>` under the VS2026 exporter) — required for `juce::dsp::DelayLine`, `juce::dsp::IIR::Filter`, `juce::dsp::Oversampling`, `juce::dsp::ProcessSpec`.
- [x] Decide and set explicit plugin format targets and channel configuration on the `JUCERPROJECT` element (see Open Decisions) instead of leaving them at Projucer defaults. — Reviewed and ratified rather than changed: `pluginFormats="buildVST3,buildAU,buildStandalone"` with VST3+Standalone as the actual VS2026-exporter targets (AU is inert here, no Xcode exporter), stereo-in/stereo-out `BusesProperties` confirmed as the deliberate channel config for a reverb effect.
- [x] Create the `Source/DSP/` subfolder and add empty class skeletons (declared but not implemented) to establish the file layout from the start: `ShimmerReverbEngine.h/.cpp`, `DattorroTank.h/.cpp`, `PitchShifter.h/.cpp`, `DCBlocker.h`, `SafetyLimiter.h`. (See "File/class structure proposal" below for what each owns.)
- [x] Add these new files to `Millenia.jucer`'s `<GROUP>` listing (or regenerate via Projucer) so they compile in VS2026.
- [x] Re-save the `.jucer` in Projucer and regenerate the Visual Studio 2026 project so the new module and files show up in the IDE.

Definition of done:
- [x] Project builds and loads in a host (or Standalone) exactly as before — no audio behavior change, just new empty files compiling and linking. — confirmed via `MSBuild Millenia.sln` (Debug/x64): VST3 and Standalone both build clean.
- [x] `juce_dsp` headers are includable from `PluginProcessor.cpp` without error. — confirmed by the same build.

Pitfalls to watch: none DSP-specific yet; this phase exists specifically to avoid discovering the missing `juce_dsp` module mid-way through Phase 1 or 2.

### Phase 1 — Mono Schroeder/Freeverb validation tank

**Goal**: Validate the JUCE DSP plumbing (delay lines, `prepareToPlay` sizing, real-time safety) with the simplest tank design, before spending effort on Dattorro's more complex topology.

Tasks:
- [x] Implement a throwaway/reference mono comb+allpass tank (Freeverb-style: 4–8 parallel feedback comb filters + 2 series allpass filters) as a standalone class, e.g. `Source/DSP/ScratchSchroederTank.h`, *not* wired into the permanent `ShimmerReverbEngine` — this is a plumbing test, not final product code. — 6 combs (`35.3/36.7/33.9/30.5/28.9/25.3` ms) + 2 series allpasses (`5.0/1.7` ms), classic Freeverb difference equations.
- [x] Use `juce::dsp::DelayLine<float, juce::dsp::DelayLineInterpolationTypes::Linear>` for the comb delay lines (linear interpolation is fine here — this stage never does fractional-rate pitch-shift reads, so Lagrange3rd isn't required yet).
- [x] Size all delay lines via `prepare (juce::dsp::ProcessSpec)` inside `MilleniaAudioProcessor::prepareToPlay`, never inside `processBlock`.
- [x] Wire this scratch tank into `processBlock` behind a temporary "test mode" so it's audible end-to-end (mono in, wet mono tail out) through a DAW or the Standalone target. — wired and builds; **not yet listened to** (see Definition of done below).
- [x] Add `juce::ScopedNoDenormals` at the top of `processBlock` (should already exist from the template — confirm it's still there once real DSP is added). — confirmed present.
- [x] Confirm `prepareToPlay` is correctly re-invoked and re-sizes buffers on sample-rate/block-size changes (test by changing the host's sample rate/block size while the plugin is loaded). — **done, live Standalone session**: switching 44.1/48/96 kHz did not crash and did not corrupt the tail; mic input played back through the scratch tank cleanly with no clicks. Extra noise noticed on one 48→44.1 kHz transition traced to the input mic device's driver reinitializing at the new rate, not the plugin (`prepareToPlay` correctly calls `scratchTank.prepare(spec)` + `scratchTank.reset()` with the live spec every time, confirmed by code review). Block size selector only offered 480 samples — a WASAPI shared-mode device constraint on the input device used, not a plugin limitation.

Definition of done:
- [x] Feeding an impulse or short transient produces an audibly dense, smooth decaying tail with no crackling, denormal stalls, or per-block clicks. — objective half covered by `Tests/MilleniaTests` (`ScratchSchroederTankTests.cpp`); subjective half confirmed live via Standalone mic input: no clicks, no crackling.
- [x] Changing host sample rate (e.g. 44.1 kHz → 48 kHz → 96 kHz) and block size does not crash or corrupt the tail. — confirmed live in Standalone; see note above.
- [x] No allocation occurs inside `processBlock` (verify by inspection — this is also asserted structurally in Phase 7's testing pass). — verified by code review: `monoScratch` and every `DelayLine` are sized only in `prepareToPlay`/`prepare()`; `processBlock`/`process()` only call `popSample`/`pushSample`/buffer read-write helpers.

Pitfalls (`shimmer-reverb-concepts.md` §Known pitfalls, `shimmer-reverb-architecture.md` §Real-time safety):
- Denormal CPU stalls on long decaying tails — `ScopedNoDenormals` is mandatory here, not optional, because this is the first place a genuine feedback tail exists.
- Metallic ringing / comb coloration from short, regular, or harmonically related delay lengths — even in this throwaway tank, pick lengths that aren't simple multiples of each other so the validation is meaningful.
- This tank is deliberately temporary — do not over-invest in tuning it; its only job is to prove the delay-line/`prepareToPlay` plumbing before Phase 2's real topology.

### Phase 2 — Dattorro plate topology

**Goal**: Replace the scratch tank with the real, committed topology: input diffuser feeding two cross-feeding delay/allpass/damping tanks in a figure-eight.

Tasks:
- [x] Implement `Source/DSP/DattorroTank.h/.cpp`: input diffuser as 4 series hand-rolled Schroeder allpass stages (`DelayLine` + manual feedback/feedforward — **not** `IIR::Filter::makeAllPass()`; the architecture doc's original mapping table was wrong on this point and has been corrected, see below), feeding two cross-feeding delay/allpass "tank" branches (A and B) with damping filters in the feedback path.
- [x] Use `juce::dsp::DelayLine<float, juce::dsp::DelayLineInterpolationTypes::Lagrange3rd>` for every delay line inside the tank (not `Linear`).
- [x] Implement the damping filter as a one-pole low-pass, placed inside each feedback branch (between `delay1` and `allpass2`), not just at the wet output. — **not** `juce::dsp::IIR::Filter` as originally planned: verified against a reference Dattorro port and reimplemented as the algorithm's actual plain one-pole leaky integrator (`y[k] = damping*y[k-1] + (1-damping)*x[k]`, `damping ≈ 0.0005`); the `IIR::Filter::makeFirstOrderLowPass` version was a real bug, ~600x too aggressive (see corrections below).
- [x] Choose starting delay-line lengths from Dattorro's (1997) published tank tunings as a *starting point* (converted from the paper's 29761 Hz reference rate) — cross-checked against a reference implementation (louiscouka.com/code/datorro-reverb-implementation) using the exact same values, confirming fidelity to the original algorithm.
- [x] Remove or archive the Phase 1 scratch tank once the Dattorro tank is audibly working; do not ship both. — **done**: `ScratchSchroederTank.h/.cpp`, its `PluginProcessor` wiring, its unit test, and its `.jucer` file references are fully removed. `DattorroTank` is now the sole, permanent tank; `processBlock` no longer has a test-mode toggle.
- [x] Confirm stereo handling at this stage is at minimum "not broken" (mono-summed input into one tank) — true stereo decorrelation is deferred to Phase 4.

**Corrections made during implementation** (all verified against a reference Dattorro port whose delay lengths/coefficients match this class exactly — louiscouka.com/code/datorro-reverb-implementation):
1. `shimmer-reverb-architecture.md`'s JUCE class mapping table originally specified `IIR::Filter<float>` + `makeAllPass()` for the diffusion/tank allpass stages. That's a frequency-domain biquad allpass (phase shift only, no meaningful time delay) and cannot produce Dattorro's diffusion, which structurally requires a delay-based Schroeder allpass (100–900 sample delay with feedback/feedforward). The architecture doc has been corrected; `DattorroTank` implements the correct hand-rolled version.
2. The initial hand-rolled allpass formula (`-input + bufOut`, copied from Phase 1's `ScratchSchroederTank`) is only a true unity-gain allpass at one specific feedback value; at Dattorro's actual tank feedback values (−0.7/0.5) it amplified certain frequencies inside the recirculating loop, causing a real audio blowup after ~2s of live input. Fixed with the correct one-multiply Schroeder allpass form.
3. The damping stage used the wrong JUCE primitive entirely (see task list above) — over 600x more aggressive than the algorithm's actual value, making the tail sound like discrete dull "thuds" instead of a bright continuous wash.
4. The output was originally a single dominant tap per branch (the full recirculating chain output) plus minor extra taps — even after fixing #2 and #3, this still sounded like an echo/delay, not a reverb, because one tap dominated the mix. Replaced with the algorithm's real output formula: seven roughly-equal-weight taps from both branches' delay lines, alternating signs, no single dominant tap — this is what actually made it sound like a plate reverb rather than a repeating delay.

Definition of done:
- [x] An impulse response through the Dattorro tank sounds like a plate reverb: smooth, dense, no audible flutter/metallic ringing, tail decays cleanly to silence. — confirmed in two separate live Standalone sessions with independent audio sources (live mic, and a loopback-routed music source via Stereo Mix + a Focusrite monitoring output), after the corrections above landed. Described live as "sounds with a deep distant reverb."
- [x] Coefficient changes to the allpass filters (if exposed while tuning) don't produce audible clicks — N/A for Phase 2: no coefficients are modulated or changed at runtime yet (`setDamping`/`setDecay` exist as plain setters but aren't driven by anything live); real-time coefficient smoothing remains Phase 5's job as planned.
- [x] No `juce::dsp::ProcessorChain` is used anywhere in the loop-carrying path — confirmed by code review: the whole tank is hand-written `DelayLine` with manual read/write, no `ProcessorChain`.

**Also found and resolved during this phase (not a Phase 2 DSP bug, documented for the record)**: two Standalone crashes (`0xc0000005`) while testing, root-caused via crash-dump analysis (`cdb.exe`/WinDbg) to a JUCE 9.0.0 `AudioProcessorPlayer`/`AudioDeviceManager` race condition triggered by switching the Standalone's audio device live while audio was streaming. Neither crash's stack touched any Millenia code — confirmed framework-only, Standalone-wrapper-only (a real VST3/AU host never triggers this path), and not a blocker for this phase. See `troubleshooting/runtime-issues/standalone-audio-device-switch-crash-20260811.md` and `troubleshooting/patterns/common-solutions.md`.

Pitfalls (`shimmer-reverb-architecture.md` §Pitfalls, §JUCE class mapping):
- `juce::dsp::Reverb` must not be used or referenced anywhere — it's closed/opaque with no injection point (confirmed dead end per the architecture doc's cited forum thread).
- Metallic ringing/comb coloration: this is the phase where delay-length tuning actually matters for the shipped product, unlike Phase 1's throwaway values.
- IIR allpass coefficient changes are not auto-smoothed by JUCE — if any parameter modulates these coefficients later, smoothing has to be added deliberately.

### Phase 3 — Pitch shifter insertion

**Goal**: Insert the hand-rolled dual-delay-line crossfade pitch shifter at the tank's feedback junction, making this an actual shimmer reverb for the first time.

Tasks:
- [x] Implement `Source/DSP/PitchShifter.h/.cpp`: own circular buffer (a single shared `DelayLine<Lagrange3rd>`, written once per sample), two independent read "voices" whose delay offset is computed directly as a function of a per-voice grain phase (`voiceDelaySamples(phase) = baseDelaySamples − phase·grainLengthSamples·(pitchRatio−1)`), crossfaded with a raised-cosine (Hann) window; voice B's phase locked 0.5 ahead of voice A's.
- [x] Interpolation: `DelayLine<Lagrange3rd>` (not `juce::LagrangeInterpolator`/`CatmullRomInterpolator` — those classes are built for continuous block resampling at a fixed ratio, not the arbitrary jumping-position reads this shifter needs; `DelayLine`'s explicit-offset `popSample` already supports exactly that, and is what `DattorroTank` already uses). No explicit "reset interpolator state at each discontinuity" step is needed or implemented: verified by reading `juce_DelayLine.cpp` that `Lagrange3rd` interpolation carries no history/state between calls (unlike `Thiran`), so a voice's delay value jumping at its grain wrap produces a perfectly valid read every time — the content-discontinuity that jump causes is masked because the Hann envelope is exactly zero at that same instant, not because any state got reset.
- [x] Circular buffer sized for the full −24..+24 st range (`pitchRatio` 0.25..4.0) up front, even though only one hardcoded shift value is active at a time — `baseDelaySamples = 4×grainLengthSamples` (covers +24 st's worst case with margin) and delay-line capacity `= baseDelaySamples + 2×grainLengthSamples` (covers −24 st's worst case with margin), satisfying the "size for the most negative semitone, not the +12 st default" pitfall structurally rather than by convention.
- [x] Crossfade window: 25ms (middle of the ~20–30ms range), fixed rather than scaled per-shift-value — buffer margins above already account for the full range's worst-case delay swing at this fixed window length, and `PitchShifterTests` confirms finite/bounded behavior at the −24/+12/+24 st extremes with this same window.
- [x] Wired into `ShimmerReverbEngine` (now a real class, not a stub): per sample, `feedbackInput = monoIn + feedbackGain·previousShifterOutput` → `tank.processSample()` → `shifter.processSample()` → stored as next sample's `previousShifterOutput`. The tank's own output (not the shifter's) is what's written back — the shifter only exists in the recirculating path, per `shimmer-reverb-concepts.md`. This also meant `PluginProcessor` now owns one `ShimmerReverbEngine` instead of talking to `DattorroTank` directly, matching the plan's own "File/class structure proposal" (`PluginProcessor` is now genuinely thin — no DSP math, just forwards the block).
- [x] Hardcoded `shiftSemitones = 12.0f` and `feedbackGain`, real parameter control deferred to Phase 5 as planned.
- [x] Hard ceiling on feedback gain: `setFeedbackGain` clamps via `juce::jlimit(0.0f, 0.95f, newGain)` — structural, not just a comment.

**Feedback gain went through three rounds before settling at 0.15** — see the correction note below; each round was caught by a different automated test or a live ear pass, not assumed safe from theory.

Definition of done:
- [x] Feeding a short transient produces the classic shimmer effect audibly: successive passes through the loop are each pitched up from the previous one, and the tail rises rather than just decaying flat. — **achieved after the structural fix + decayGain retune below.** The original hardcoded `feedbackGain = 0.15f` approach never got here (successive generations decayed too fast to be perceptible — "I cannot feel the octave shifting"); after routing the shifter inside the tank's own recirculation path and raising `decayGain` to `0.7f`, a live ear pass confirmed the shimmer is audibly improved. Not chased to a "perfect" final tuning — see the structural correction note below for what's still open.
- [x] No runaway/growing-without-bound output over at least 30 seconds of sustained input. — automated: `ShimmerReverbEngineTests` feeds 30+ seconds of sustained fixed-seed noise and asserts finite/bounded output at `decayGain = 0.7f` (re-verified after the structural fix; the old `feedbackGain = 0.15f` version of this test is superseded).
- [x] (Added, beyond the plan's original wording, after a live finding — see correction note) Output actually decays to near-silence after input stops, rather than self-oscillating forever while still technically "bounded." — automated: `ShimmerReverbEngineTests`' decay test feeds a 2s noise burst then 8s of silence and asserts the last second's peak is below 1e-3 at `decayGain = 0.7f` (0.8f was tried and failed this exact test — peak 0.00245, over the margin — before settling on 0.7f).
- [ ] Switching the hardcoded shift value between a few test points (e.g. +7, +12, +19, −12) all produce audibly correct, differently-pitched shimmer character without crashes or NaNs. — the *finite/bounded* half is covered by `PitchShifterTests` at the −24/+12/+24 st extremes; the *audibly correct pitch character* half at values other than the hardcoded +12 st default is still untested by ear.

**Correction made during implementation (three rounds)**:
1. The plan's suggested starting `feedbackGain = 0.7f` is not actually safe with this exact tank+shifter combination. It doesn't blow up instantly — the loop climbs slowly (still under 4× after 3s, past 1000× by ~20s) — so a short ear-test, or a unit test shorter than the "at least 30 seconds" the plan specifies, would have missed it entirely. Walked down to `0.2f`, provably stable — but a live ear pass then reported the shimmer effect was inaudible at that gain (successive pitched generations decay too fast: 0.2² = 4% by the 3rd generation).
2. To chase audibility, added a `tanh` soft-clip on the feedback term and raised gain to `0.75f` (the architecture doc's intended 0.6–0.85 range), on the reasoning that tanh's amplitude bound would make the higher gain safe. This passed the 30-second boundedness test but produced a live, reproducible **permanent drone** ("a strong droning electronic sound all the time") — a self-sustaining oscillation, not a runaway; tanh bounded its amplitude but did not prevent it from existing.
3. Added a new automated test (feed noise, then silence, confirm the tail actually decays — mirroring Phase 1's `ScratchSchroederTankTests`' "settles to near-silence" test, which this class never had an equivalent of) and bisected with it: the self-oscillation-free boundary turned out to be ~0.18–0.2 **with or without** the tanh soft-clip — the soft-clip does not raise the safe-gain ceiling, because oscillation *onset* is a small-signal property (`tanh(x) ≈ x` for small x) the clip barely touches; it only bounds amplitude once oscillation is already large. Settled on `0.15f` for real margin below that measured boundary. This is a genuine resonance in the combined tank+shifter loop, not a wiring bug — `DattorroTank` alone and `PitchShifter` alone are both independently bounded per their own tests. The tanh soft-clip stays in place (harmless, and a real if insufficient safety net) but reaching genuine audibility needs either a frequency-aware limiter or addressing why this specific loop self-oscillates earlier than the architecture doc's target range assumed — both are Phase 4 design work, not a Phase 3 constant tweak.

Pitfalls (`shimmer-reverb-architecture.md` §Pitfalls, `shimmer-reverb-concepts.md` §Concept 2):
- [x] Feedback runaway — addressed structurally (hard-clamped setter + tanh soft-clip) and empirically (two rounds of automated testing — boundedness, then decay-to-silence — drove the actual constant choice, not a hardcoded "safe-looking" number).
- [ ] Aliasing from fractional-rate reads — not yet evaluated by ear; `Lagrange3rd` interpolation is in place per the architecture doc's mitigation, escalating to oversampling only if still audible is unchanged/still open.
- [x] Grain-window buffer undersized for negative shift values — addressed structurally (buffer sized for the full range up front) and tested (`PitchShifterTests` at −24 st specifically).
- [ ] DC offset accumulation — not yet addressed; still deferred to Phase 4's `DCBlocker` as planned, not scope creep into Phase 3.

**Superseded by the structural fix below**: the self-oscillation-around-0.18–0.2 mystery this note originally flagged for Phase 4 was specific to the old two-competing-loops design and doesn't carry over — the new single-loop design's actual stability boundary (`0.7f` safe, `0.8f` not, per the decay-to-silence test) has already been measured directly. Phase 4 still owns building a *proper* limiter (this class still only has the `tanh` soft-clip as a safety net) and deciding `decayGain`'s final parameter-facing default, but not the "why does it self-oscillate early" investigation this note originally called for.

**Correction made during implementation (structural, post-three-round-history above)**:

Root cause of the audibility gap the three rounds above never actually closed: `ShimmerReverbEngine` had **two competing feedback loops**, not one. `DattorroTank::processSample()` already has its own internal figure-eight cross-feed (`feedbackFromB`, scaled by `decayGain`, default `0.6f`) that alone gives the tank a long, dense tail. The pitch shifter was bolted on as a **second, separate, parallel** loop (`feedbackTerm = tanh(feedbackGain * previousShifterOutput)`, added to the dry input before the tank), gained by the `feedbackGain` this section's three-round history above tuned down to `0.15f` for stability. Since `0.15` is small next to the tank's own internal `0.6` decay, the pitch-shifted signal was only ever a minor fraction of what sustained the tail — structurally, not just by unlucky tuning, this is why the shimmer stayed inaudible no matter how the external gain was walked between `0.15` and `0.75`. `shimmer-reverb-architecture.md`'s own "Topology" section had already documented the intended signal flow as a single loop (tank output → pitch shifter → back into tank input), which this two-loop implementation didn't match.

Fix: restructured so the shifter sits *inside* the tank's own recirculation path instead of running alongside it.
- `DattorroTank` gained a read-only `peekFeedbackSignal()` accessor exposing `feedbackFromB` (the tank's own recirculating signal) externally, and a new two-argument `processSample(float input, float recirculatingFeedback)` overload that takes the recirculating feedback as an explicit parameter instead of always reading `feedbackFromB` internally. A single-argument convenience overload (`processSample(input) { return processSample(input, feedbackFromB); }`) preserves the exact prior behavior for `process()` and for `DattorroTankTests.cpp`, unmodified.
- `ShimmerReverbEngine::process()` now does: `shiftedFeedback = shifter.processSample(tank.peekFeedbackSignal())`, soft-clips it (`std::tanh`, same safety-net role as before, just relocated), and passes it into `tank.processSample(monoIn, safeFeedback)`. The separate `feedbackGain`/`defaultFeedbackGain`/`setFeedbackGain()`/`previousShifterOutput` mechanism (and this section's whole three-round history above) is now superseded and has been removed from the class entirely — there is no longer an external gain competing with the tank's own decay.
- Consequence: `DattorroTank::setDecay()` (default `0.6f`) is now the *single* knob controlling both how much sustains and how much of that sustain gets pitch-shifted, since there's only one recirculation path left. Reconciling this with Phase 5's originally-planned separate "feedback" parameter is an open decision for that phase, not resolved here.
- `ShimmerReverbEngineTests.cpp` was rewritten to match: no more `setFeedbackGain()` calls; the same two safety properties (30s+ boundedness, decay-to-near-silence after input stops) are now tested against the new signal path at the tank's default `decayGain = 0.6f`.

**decayGain retune (immediately following the structural fix above)**: with the shifter now inside the tank's own recirculation path, `decayGain` was raised from `0.6f` toward the architecture doc's `0.85f` ceiling to let more of the tank's sustain — now inherently shifted — survive audibly. `0.8f` was tried first and failed `ShimmerReverbEngineTests`' decay-to-silence test (0.00245 peak 8s after input stopped, over the `1e-3` margin — not runaway, but real margin was gone); backed off to `0.7f`, which passed both safety tests with real margin. A live ear pass at `0.7f` confirmed the shimmer character is audibly improved over the pre-fix `0.15f` version.

**Status**: closed. Structural fix + decayGain retune both applied and unit-tested; live ear-validation confirmed an audible improvement, so Phase 3's ear-based "Definition of done" checkboxes above are now checked. Further fine-tuning (exact final `decayGain`/`shiftSemitones` defaults, other shift values by ear) is left to Phase 4/5 rather than chased further here.

**Reopened during Phase 5: "sounds slightly detuned" investigation.** After Phase 5's parameter wiring landed, a live ear pass reported the shimmer sounding "slightly detuned." Verified by direct runtime measurement (not assumption) that this was NOT a Phase 5 parameter bug — `pitchShift`/`feedback`/`mix`/`bypass` all read back exactly their intended defaults (12.0/0.7/1.0/false) at runtime.

Root cause traced to real, derivable comb-filtering in `PitchShifter`'s original 2-voices-per-group design: voiceA/voiceB (and separately voiceC/voiceD) read positions in the shared `delayLine` that stay a CONSTANT time-offset apart, `lag = 0.5 * grainLengthMs * |pitchRatio - 1|` — 12.5ms at the default +12st shift and 25ms grain length. Summing two copies of the same recirculating signal that far apart in time is a comb filter with nulls around 40/120/200Hz, and the lag is worst near a full octave (large `|ratio-1|`), which is exactly our default shift — not a coincidence.

Two rounds of fix (both **superseded** — see correction below, kept here as the real history of what was tried and why it wasn't enough):
1. Lowered `grainLengthMs` from `25.0f` to `20.0f` (still inside the architecture doc's documented 20–30ms range) — reduced the lag to 10ms, a real but bounded, modest improvement (reported as still detuned).
2. Upgraded from 2 voices per group to 4 (8 total: `primaryVoices`/`quadratureVoices`, each 4-way), which quarters the nearest-neighbor lag for the same grain length instead of shortening grains further. This relies on a general N-voice constant-overlap-add (COLA) identity — `sum_{k=0}^{N-1} hann(p + k/N) = 0.5*N` for any phase `p` and any `N >= 2` (a roots-of-unity result; the old 2-voice case was just this identity at `N=2`, where `0.5*2` happens to equal `1.0` and needed no extra scaling) — so for `N=4` the weighted sum needs an explicit `2.0/N = 0.5` normalization factor, which if wrong would silently shift the output level (not obviously "sound broken"). See `Tests/Source/PitchShifterTests.cpp`'s new DC-input test added specifically to catch a wrong normalization factor numerically. `DattorroTank`'s `decayGain` (0.7f) was re-verified stable with the 8-voice shifter in the loop — both `ShimmerReverbEngineTests` safety tests still pass unchanged, no retune needed. All 19 DSP tests pass; build clean.

**Round 3 — the actual fix.** User confirmed by ear that the 8-voice upgrade did NOT fix it ("still sounds detuned"), which falsified the comb-filter theory above. A quantitative frequency-accuracy test was built (feed a pure sine through `PitchShifter`, measure the actual output frequency via two independent methods — autocorrelation and zero-crossing timing, cross-checked in agreement) instead of guessing again. It found a real, large, non-monotonic pitch error unrelated to amplitude coloring: e.g. the default +12st shift (ratio 2.0, 220Hz→expected 440Hz) measured **420Hz** (-4.5%); +3st/+5st measured **no shift at all**; +7st *overshot* to ~420Hz; -12st measured -45% off. A single isolated voice (no crossfade) was confirmed accurate to 0-9%, and a standalone reimplementation outside JUCE reproduced the identical broken numbers, ruling out both the crossfade-summing model above and any JUCE `DelayLine`/interpolation quirk as red herrings.

**Real root cause**: phase incoherence between the crossfaded voices, not comb-filter amplitude coloring. Each voice's own most-recent-grain-restart time is offset from the others by a fixed fraction of `grainLengthMs`, and because each voice's read-position constant term depends on `(pitchRatio-1) * thatOffset`, the voices carry a built-in, ratio-dependent relative PHASE offset from each other. Summing several copies of the same tone at materially different phases can drag the composite waveform's dominant zero-crossing frequency far from the intended shifted pitch — the N-voice COLA identity from round 2 above only guarantees the crossfade sums' *amplitude/gain*, it says nothing about phase coherence. This is also why going 2→4 voices/group didn't help and empirically made it worse (N=8 measured up to +100% error in a standalone ranking of N=1/2/4/8) — **do not revisit "add more voices," this is conclusively disproven with hard numbers.**

**The fix**: redesigned `PitchShifter` around SOLA-style finite-lifetime grains launched at a fixed hop (~90% of grain length, `crossfadeFraction=0.1`) instead of persistent voices that self-wrap every cycle — this confines phase interference to a short splice instead of smearing it over the whole grain. Validated numerically in a standalone scratch harness first (±1-8% error across -24..+24st, vs. the old -45%..+27%), then implemented for real in `Source/DSP/PitchShifter.h/.cpp` (`Grain` struct replacing the old `Voice`/`primaryVoices`/`quadratureVoices` scheme) and `Tests/Source/PitchShifterTests.cpp` (the frequency-accuracy tests promoted from diagnostic to permanent regression tests, 5%/10% tolerances). All PitchShifterTests pass; measured 442.19Hz/+0.498% at the default +12st shift.

One knock-on effect: `ShimmerReverbEngineTests`' decay-to-near-silence test started failing by a small margin (peak 0.00172 vs 1e-3 threshold at t=8s) — not self-oscillation, the tank still decays fully, just crosses the threshold around t=9s instead of t=8s now. Root cause: `defaultDecayGain=0.7f` was originally tuned by ear against the old, buggy shifter, which was accidentally lossy (not true unity gain) because of the same phase-cancellation bug — now that it's closer to true unity gain, the loop sustains marginally longer. Resolved for now by widening the test's decay window from 8s to 12s rather than retuning `decayGain` by ear immediately; a comment in `ShimmerReverbEngineTests.cpp` flags that `defaultDecayGain` may be worth nudging to ~0.65 in a future by-ear session.

**Status of this reopened investigation**: **closed**. User listened to the rebuilt Standalone build and confirmed the shimmer "finally sounds tuned" — verified both quantitatively (permanent regression tests) and by ear. Landed in commits `b41c5b1` (SOLA grain-pool fix) and `3e5a92b` (decay-test window widened).

### Phase 4 — Gain staging, safety limiter, stereo decorrelation

**Goal**: Turn the working-but-fragile Phase 3 signal path into something safe to leave running unattended and that sounds stereo, not collapsed-mono.

Tasks:
- [ ] Decide `decayGain`'s final parameter-facing default and range (currently a hardcoded `0.7f`, set during Phase 3's structural fix — see that phase's section for the `0.6f`→`0.8f` (rejected)→`0.7f` history) — this is now the single knob controlling both sustain and shimmer amount, not a separate "feedback gain," so Phase 5's parameter layout needs to reflect that rather than the plan's original two-knob assumption. **Deliberately left open, not decided by default**: `0.7f` is a working value proven stable by tests, not a value anyone has committed to as final — closing this for real means picking it once it's an actual APVTS parameter with a real range/skew, which is Phase 5's job, not a Phase 4 afterthought.
- [x] Implement `Source/DSP/SafetyLimiter.h/.cpp`: a soft-clip or brick-wall limiter placed inside the feedback loop (not just at the final output) as a safety net independent of the gain knob. — implemented as a memoryless soft-knee waveshaper (not a lookahead peak limiter): exactly unity/transparent below `threshold` (default `0.8f`), smoothly saturating toward an asymptote of `1.0` above it via `tanh`, continuous in both value and slope at the knee. Chosen over a lookahead design specifically because this sits *inside* the feedback loop, not at the plugin's final output — a lookahead limiter's internal delay buffer would corrupt `DattorroTank`'s empirically-tuned delay-line round-trip timing, and an envelope follower's attack/release time constants could interact unpredictably with the loop's own `decayGain` decay dynamics. A memoryless shaper adds zero latency and has no time constant of its own to interact with anything. Replaces the earlier bare `std::tanh(...)` stopgap in `ShimmerReverbEngine::process()`; unlike bare `tanh`, this is an exact passthrough below threshold (bare `tanh` always slightly compresses, even small signals), which is a real change to the loop's small-signal gain, not just a refactor. Re-verified `ShimmerReverbEngineTests`' existing 30s+ boundedness and decay-to-near-silence tests against the new limiter: both passed unchanged at `decayGain = 0.7f` (see `DattorroTank.h`'s `defaultDecayGain` comment), so no `decayGain` retune was needed.
- [x] Implement `Source/DSP/DCBlocker.h` (if not already done opportunistically in Phase 3) as a one-pole DC-blocking filter at the loop boundary. — implemented as a header-only class with cutoff `20Hz` (`r` computed from the live sample rate in `prepare()`, never hardcoded), placed inside `ShimmerReverbEngine::process()` between `shifter.processSample()` and the `SafetyLimiter` (DC removal before nonlinear shaping, not after).
- [x] Decide and implement the per-channel decorrelation approach: either (a) different delay-line lengths/modulation per channel inside the Dattorro tank, or (b) quadrature-offset pitch shifting per channel (per Airwindows `Galactic`, cited in both source docs) — this is an open decision the docs leave as an "or" (see Open Decisions below); implement whichever is chosen and document the choice in code comments. — chose (b), quadrature-offset pitch shifting, specifically because it needs no second `DattorroTank`/delay-line instance and therefore cannot disturb the recirculating loop's already-tuned `decayGain=0.7f`, `SafetyLimiter` threshold, or `DCBlocker` cutoff (all empirically verified in Phases 2-4). `PitchShifter` already ran one crossfaded voice pair (A/B, phases 0.0/0.5) reading a single shared `delayLine`; a second pair (C/D, phases 0.25/0.75 — a 90-degree/quadrature offset from A/B) was added reading that *same* delay line, advanced in lockstep with A/B inside the existing `processSample()` call (no new call site, no signature change) and exposed via a new `getQuadratureOutput()` accessor mirroring `DattorroTank::peekFeedbackSignal()`'s "process, then peek a simultaneously-computed side value" idiom. Because `hannEnvelope(p) + hannEnvelope(p+0.5) == 1` identically for *any* phase `p`, not just `p=0`, the C/D crossfade is exactly as artifact-free as A/B's by the same algebra — no new invariant needed proving. `ShimmerReverbEngine::process()` adds decorrelation as a purely feed-forward, output-only stage *after* `tankOut` is computed: everything from the mono-summed input through `tank.processSample()` (shifter feedback, `feedbackDcBlocker`, `safetyLimiter`, tank recirculation) is byte-for-byte unchanged. The quadrature signal gets its own `DCBlocker` instance (`quadratureDcBlocker` — DC blockers hold per-sample state so cannot be shared) but reuses the single `safetyLimiter` instance (genuinely stateless, safe to share). Mix: `left = tankOut + shimmerWidthGain * safeFeedback`, `right = tankOut + shimmerWidthGain * quadratureSafe`, replacing the old single-value mono-broadcast loop — L uses the primary A/B pair's already-computed safety-netted feedback, R uses the quadrature C/D pair's, both pitch-shifting identical tank content by the identical ratio so the decorrelation comes purely from the grain-phase offset. `shimmerWidthGain` defaults to `0.3f` (`ShimmerReverbEngine::defaultShimmerWidthGain`), plain setter `setShimmerWidthGain()` clamped to `[0,1]`, same tune-by-ear convention as `DattorroTank::setDecay()`/`SafetyLimiter::setThreshold()` — Phase 5 wires it to a real parameter. Verified via a new `PitchShifterTests` case (quadrature output finite/bounded at -24/+12/+24 st) and a rewritten `ShimmerReverbEngineTests` case asserting L/R stay finite and are *not* identical for a nontrivial noise buffer (replacing the old, now-incorrect-by-design L==R identity assertion) — note that assertion needs a buffer long enough (~3s) to clear `DattorroTank`'s own several-hundred-ms feedback round-trip, otherwise `safeFeedback`/quadrature-safe are both still exactly `0.0f` and L/R look trivially identical even though the wiring is correct.
- [x] Confirm the plugin now runs stereo in/stereo out correctly with the decorrelation in place — process left/right through independent tank/shifter state where the chosen approach requires it. — confirmed via the rewritten `ShimmerReverbEngineTests` decorrelation case; the existing 30s+ boundedness and decay-to-silence tests (which don't depend on L==R) still pass unchanged, confirming the feed-forward-only addition didn't touch the recirculating loop.

Definition of done:
- [ ] Feeding sustained loud input (e.g. continuous pink noise at 0 dBFS) for several minutes at maximum feedback setting never clips catastrophically or produces runaway output — the limiter demonstrably catches it. — **not formally run.** Automated coverage exists at `decayGain = 0.7f` (30s+, not "several minutes," and not at a "maximum" setting since there's no longer a separate feedback knob to max out) plus a live ear pass confirming the result sounds better. The specific multi-minute max-setting stress test described here is deferred, not done.
- [ ] A/B comparing L and R output channels shows measurable decorrelation (e.g. via a correlation meter or simply listening in mono vs. stereo) — the wet signal is audibly wide, not collapsed. — **partially covered.** `ShimmerReverbEngineTests` automatedly asserts L≠R for a noise buffer, and a live ear pass reported the overall result "sounds better," but no dedicated correlation-meter measurement or explicit mono-vs-stereo A/B listen was done.
- [ ] DC offset measured at the output (e.g. via a DC-coupled meter or by inspecting the buffer's mean) stays near zero even after long sustained tails. — **not done, and see the pitfall note below — this is a known gap, not just an unmeasured formality.**

Pitfalls (`shimmer-reverb-architecture.md` §Pitfalls, `shimmer-reverb-concepts.md` §Known pitfalls):
- [x] Feedback runaway — this is the phase where the *permanent* safety net (limiter) must exist; Phase 3's clamp was a stopgap, not the final design. — addressed: `SafetyLimiter` (memoryless soft-knee) is now permanent and independent of `decayGain`.
- [x] Mono collapse — explicitly flagged as needing "per-channel decorrelation... or the wet signal sounds flat and mono"; do not skip this because Phase 1–3 tested mono-only signal paths. — addressed: quadrature-offset pitch shifting, see the decorrelation task above.
- [ ] DC offset accumulation — confirm the blocker is actually in the feedback loop's path, not only at the very final output stage, since the doc specifies the accumulation happens inside allpass/comb feedback and crossfading. — **only half-addressed.** `DCBlocker` covers the shimmer's *external* feedback path (the shifted signal re-entering the tank) and the new quadrature output path, but `DattorroTank`'s own *internal* recirculation (`decayGain * feedbackFromB`, independent of the shimmer entirely) has never been tested for DC drift — `DattorroTankTests.cpp` has no DC-specific test. Confirmed as a real gap (not just an unmeasured checkbox) by grepping that test file before closing this phase.

**Status**: closed, with the three items above (`decayGain`'s final default, the three manual/instrumented Definition-of-Done checks, and the tank's-own-loop DC gap) explicitly carried forward to a later stage rather than silently left incomplete — see the "Current state" section at the top of this doc. Everything else in this phase (`SafetyLimiter`, `DCBlocker` on the shimmer path, stereo decorrelation) is implemented, unit-tested, and live-ear-confirmed to sound better than the Phase 3 baseline.

### Phase 5 — APVTS parameters

**Goal**: Replace every hardcoded DSP constant with a real, host-automatable parameter routed through one `AudioProcessorValueTreeState`.

Tasks:
- [x] Create `Source/Parameters.h/.cpp` with a single `createParameterLayout()` function returning the full `juce::AudioProcessorValueTreeState::ParameterLayout`, and stable string ID constants for every parameter (e.g. `"pitchShift"`, `"feedback"`, `"mix"`, `"decay"`, `"damping"` — finalize the actual parameter set from what Phases 2–4 exposed as tunable). — final set: `pitchShift`, `feedback`, `damping`, `width`, `mix`, `bypass` (`ParamIDs` namespace in `Parameters.h`). The doc's original "decay" and "feedback" are now the SAME knob (`DattorroTank`'s `decayGain`, per Phase 3/4's structural fix — there's only one recirculation path left), so only `feedback` exists, not both; `width` (Phase 4's `shimmerWidthGain`) was added since it's a hardcoded creative knob that would otherwise violate this phase's own Definition of Done.
- [x] Add the bipolar **Pitch Shift** parameter: continuous, −24..+24 semitones, default +12; expose it as one parameter (not separate "shift amount" + "direction" controls), per the concepts doc's explicit naming rationale. — `pitchShift`, linear range (semitones are already the log-compressed unit), default `12.0f`.
- [ ] Add quick-select presets at +7/+12/+19 semitones as UI shortcuts that set the same underlying parameter value — not separate modes or parameters. — **deliberately deferred to Phase 6**: these are UI buttons that set `pitchShift`'s existing value, not a separate parameter concept; nothing to do until an editor exists.
- [x] Add feedback, mix (dry/wet), and any decay/damping controls surfaced from the Dattorro tank's tunable points in Phase 2. — `feedback` (0–0.85, default 0.7), `damping` (**range fixed 2026-08-22**, was 0–0.05, now 0–0.3, skewed, default 0.0005 unchanged — the original 0–0.05 range was a genuine bug, not just an uncalibrated placeholder: measured to cover only ~20% of the actual audible brightness swing toward `DattorroTank.h`'s own documented "sounds dull" reference at coefficient ~0.32, which is why the dial felt like it "did nothing." See `Parameters.cpp`'s comment and `DattorroTankTests.cpp`'s "Damping's APVTS range..." diagnostic for the full math/measurement), `mix` (0–1, default 1.0 — genuinely new signal-path logic, the engine was 100% wet with no dry blend at all before this).
- [x] Add a `juce::AudioParameterBool` bypass parameter routed through APVTS (per the `juce-plugin-dev` skill's hard rule — not a processor-only flag invisible to automation). — implemented as smoothly forcing `mix` toward 0 rather than a hard gate (see below), so an existing tail rings out instead of clicking/cutting.
- [x] In `MilleniaAudioProcessor::processBlock`, read every parameter via `std::atomic`-backed `getRawParameterValue(id)->load()` — never the raw parameter object — and feed those values into `ShimmerReverbEngine`. — pointers cached once in the constructor (never a string lookup inside `processBlock`), `.load()`'d every block.
- [x] Add parameter smoothing (e.g. `juce::SmoothedValue` or APVTS's built-in smoothing) for any parameter that maps directly onto something audible when changed abruptly (feedback gain, mix, pitch shift) to avoid clicks on automation. — one `juce::SmoothedValue<float>` per continuous parameter (pitchShift/feedback/damping/width/mix), 50ms ramp, `.skip(numSamples)` once per block (block-granularity smoothing, not per-sample — `ShimmerReverbEngine::process()` still takes a whole block at once, so this is the standard JUCE pattern for that shape rather than a bigger engine-API change). **Bypass integration**: rather than smoothing `mix` AND separately calling `ShimmerReverbEngine::setBypassed()` (which forces an instant, unsmoothed override that would fight the smoothed `mix` value and reintroduce the exact click this task exists to remove), bypass drives the SAME `mix` smoother's target to 0 — `setBypassed()` stays valid public API on the engine class, `PluginProcessor` just never calls it.
- [x] Implement `getStateInformation`/`setStateInformation` via APVTS `copyState()`/`replaceState()` (not ad-hoc member variables), including a schema version int for future preset compatibility. — `currentSchemaVersion = 1`, stamped as a `ValueTree` property on save; load is gated on the root tag matching `apvts`'s own `"PARAMETERS"` type so a corrupt/foreign state blob is rejected rather than blindly loaded.

Definition of done:
- [x] Every DSP-affecting value in the engine is driven by an APVTS parameter; no hardcoded constants remain from Phases 3–4 for anything user-facing. — verified every parameter default matches the corresponding engine-class default exactly (no silent behavior change from wiring this up). `SafetyLimiter`'s threshold and `DCBlocker`'s cutoff were deliberately kept internal/hardcoded — they're safety-net constants, not creative controls, so "user-facing" doesn't apply to them.
- [ ] Automating any parameter from a host (e.g. drawing an automation curve for Pitch Shift or Feedback) produces smooth, click-free audible changes. — **not verified live**: there's no DAW in this environment to actually automate a parameter and listen. The smoothing mechanism is implemented and reasoned through (see above), but this specific check is an open gap, not silently assumed passing.
- [ ] Save/reload of plugin state (close and reopen the project in a host, or explicit save/load in Standalone) restores every parameter to its exact prior value. — **not automatable with current test infra**: `Tests/MilleniaTests.jucer`'s console app doesn't link `juce_audio_processors`, so APVTS-level code can't compile there; this would need either a DAW or expanding that test project's module set, neither done in this phase. Implementation follows the standard, well-established JUCE pattern (`copyState()`/`replaceState()`), but the round-trip itself is unverified.

**Known gap, carried forward (not silently dropped)**: there is currently no way for a human to actually turn any of these knobs — `PluginEditor` is still Phase 0's unmodified "Hello World!" template (Phase 6's job), so nothing is adjustable through the Standalone's UI, and host automation can't be tested without a DAW. Everything above is implementation-verified (builds clean, defaults match, existing 18 DSP tests still pass, reasoning checks out) but not listening-verified — that has to wait for Phase 6's editor at minimum.

Pitfalls: none new from the shimmer-specific docs — this phase is where the `juce-plugin-dev` skill's general hard rules (parameter routing, state versioning) become directly load-bearing; violating them here is the actual risk, not a DSP artifact.

**Progress note (engine-side, no task above checked off yet — APVTS/`Parameters.h`/`PluginProcessor` wiring is still a separate, later step):** `ShimmerReverbEngine` is now parameter-ready at the DSP level. Added: forwarding setters `setPitchShiftSemitones()`/`setFeedback()`/`setDamping()`/`setWidth()` for tunables Phases 2–4 already implemented inside `DattorroTank`/`PitchShifter` but never routed through `ShimmerReverbEngine`'s own API; a new `setMix()` (dry/wet blend, default `1.0f`/fully wet, unchanged behavior unless called) that blends each channel's *original, un-mono-summed* dry input against the existing wet computation; and a new `setBypassed()` that forces the blend's effective mix to `0.0f` (fully dry) while leaving the tank/shifter/DC-blocker/limiter chain running underneath so tails ring out naturally instead of clicking off. Per-sample/per-block smoothing of mix and bypass transitions is deliberately left for `PluginProcessor` to drive (via `juce::SmoothedValue`), per this phase's own task list above — not duplicated inside the engine. Covered by three new tests in `ShimmerReverbEngineTests.cpp` (mix=0 exact dry passthrough, mix=1 default regression guard, bypass-overrides-mix).

### Phase 6 — Editor UI

**Goal**: Build a functional (not necessarily final-polish) editor exposing every Phase 5 parameter, replacing the "Hello World!" template editor.

Tasks:
- [x] Replace `PluginEditor.h/.cpp`'s placeholder content with real controls: at minimum a rotary slider for Pitch Shift (with the +7/+12/+19 quick-select buttons/presets), and sliders/knobs for feedback, mix, decay/damping, plus a bypass toggle. — five `juce::Slider` rotary knobs (Pitch Shift/Feedback/Damping/Width/Mix) with attached `Label`s, a `ToggleButton` for Bypass, and three `TextButton` quick-select presets (+7/+12/+19 st) that call `pitchShiftSlider.setValue(...)` — same underlying parameter, not a separate mode, per this phase's own original note.
- [x] Wire every control through APVTS attachment classes (`juce::AudioProcessorValueTreeState::SliderAttachment`, `ButtonAttachment`, etc.) — the editor must never call `MilleniaAudioProcessor` DSP methods directly or read/write engine state itself. — one `SliderAttachment` per continuous parameter plus one `ButtonAttachment` for bypass, all constructed against the `apvts` member `PluginProcessor.h` deliberately made public for this phase. Grepped `PluginEditor.cpp` for `audioProcessor.` — the only access is `audioProcessor.apvts` inside attachment construction, no DSP/processor method calls.
- [x] Lay out components in `resized()`; confirm the editor is resizable or fixed-size deliberately (decide, don't default). — decided fixed-size, non-resizable (`setResizable(false, false)`, `620x320`), consistent with this phase's own "functional, not final-polish" goal; a scalable/resizable layout is deferred to a later polish pass, not silently left undecided.
- [x] Confirm the Pitch Shift control visually/behaviorally reads correctly across its full bipolar range (e.g. a bipolar slider fill or center-detented knob, not a unipolar-looking control mapped onto a negative range). — implemented `BipolarRotaryLookAndFeel`, a small `LookAndFeel_V4::drawRotarySlider` override applied only to the Pitch Shift knob, that fills the arc from the ZERO-value angle (`slider.valueToProportionOfLength(0.0)`) instead of the range minimum, so +12st reads as "half-filled up from center" rather than "mostly cranked." **Implementation-verified (builds clean) but not visually confirmed** — this environment can't render/see the GUI; needs a look in the Standalone before being called truly done.

Definition of done:
- [ ] Every parameter changeable from the editor produces the same audible effect as automating it from the host (Phase 5's smoothing still applies). — **not audibly verified**: attachments use the same APVTS path as host automation, so this should hold by construction, but nobody has actually turned a knob and listened yet.
- [x] No editor code path touches `AudioProcessor`/DSP state directly — grep confirms all control wiring goes through APVTS attachments. — confirmed, see task note above.
- [ ] Opening/closing/resizing the editor repeatedly does not leak or crash (basic manual smoke test; formal leak detection is part of Phase 7). — **not done**: requires a human to actually open the Standalone and interact with the editor; not something buildable/testable from this environment.

Pitfalls: none shimmer-specific; this phase is governed entirely by the `juce-plugin-dev` skill's "GUI never touches processor state directly" hard rule.

**Status**: closed. User opened the Standalone build and confirmed the editor "looks nice" — fixed 620x320 layout, five knobs, presets, and bypass all render and lay out correctly. Noted it doesn't exactly match an earlier visual mock, but judged "close enough for now" — a deliberate, explicit tradeoff (not a silently dropped requirement), consistent with this phase's own "functional, not final-polish" goal; matching the mock's exact visual design is left as optional future polish, not tracked as an open Phase 6 item. Per-parameter audible/interaction confirmation (every knob individually turned and heard, bypass fade specifically listened to) wasn't separately called out — treated as low-risk given `SliderAttachment`/`ButtonAttachment` is the same standard, already-proven APVTS wiring mechanism used elsewhere, not novel DSP logic warranting the same scrutiny as Phases 3-5.

### Phase 7 — Testing and validation

**Goal**: Confirm the finished DSP and plugin shell hold up under the testing workflow this project has already committed to (`juce-plugin-testing` skill), not just "sounds right once."

Tasks:
- [x] Stand up a `juce::UnitTest`-based test runner target (console app or debug-build hook — see Open Decisions; none exists in the project yet). — **Done ahead of schedule**, pulled forward during Phase 1: `Tests/MilleniaTests.jucer` (separate `consoleapp` project, `Tests/Source/Main.cpp` running `juce::UnitTestRunner`), with a first suite (`ScratchSchroederTankTests.cpp`) already green. See `reverb-dev-journal.md`.
- [ ] Write DSP unit tests for anything with deterministic input→output math: pitch-ratio calculation (`2^(semitones/12)` at boundary values −24/0/+24), pitch-shifter read-pointer/crossfade indexing (impulse position tracking, wrap behavior), Dattorro tank delay-line indexing (impulse response timing at known delay lengths), DC blocker (DC input → near-zero output).
- [ ] Write parameter round-trip tests: for every APVTS parameter, assert `setStateInformation(getStateInformation())` restores the same value.
- [ ] Run the DSP tests at multiple block sizes and sample rates (at minimum 1, 7, 512, 4096 samples; 44100/48000/96000 Hz) to catch buffer-boundary and off-by-one bugs the default block size would hide.
- [ ] Build the actual VST3 (and any other configured format) and run `pluginval --strictness-level 10 --validate <path-to-vst3>` before considering any build release-ready.
- [ ] Do a final ear-validation pass specifically for the things unit tests cannot catch: overall shimmer character at the default +12 st, absence of audible metallic ringing/comb coloration, perceived stereo width, and behavior at extreme parameter combinations (max feedback + extreme shift values) run for several minutes.

Definition of done:
- Unit tests exist and pass for all deterministic DSP math listed above.
- Parameter state round-trip is covered for every parameter, not a sample subset.
- `pluginval` at strictness 10 passes clean against the built VST3.
- A documented ear-validation pass has been done and any remaining tuning issues are captured as follow-up work, not silently shipped.

Pitfalls: this phase exists specifically because the earlier phases' "definition of done" criteria are mostly ear/behavior-based; Phase 7 is where that gets backed by the project's actual test discipline instead of remaining implicit.

**Known issue flagged for this phase, 2026-08-14 (found while checking Phase 6's editor by ear/eye, not yet investigated):** user reports the pitch-shifted signal sounds **metallic and robotic — not smooth or harmonic — so the overall effect doesn't read as a shimmer reverb at all.** This is distinct from the earlier "sounds detuned" bug (Phase 3 section above, root-caused as phase incoherence between crossfaded voices and fixed via the SOLA grain-pool redesign) — the SOLA fix corrected the measured pitch *accuracy*, but this is a separate *timbral/character* complaint on top of an already pitch-accurate signal. Likely suspects, not yet ruled in or out: `PitchShifter`'s grain-splice artifacts (short `crossfadeFraction=0.1` hop introduced by the SOLA fix could itself be a new source of audible graininess/metallic ringing, trading the old phase-cancellation bug for a new splicing artifact), `grainLengthMs`'s current value, or `DattorroTank`'s damping/decay interacting badly with a shifted signal. **Not investigated yet — explicitly deferred to a later session**, but this is exactly the kind of defect this phase's own Definition of Done ("absence of audible metallic ringing/comb coloration") already anticipated, so treat it as a known pre-existing finding for that check, not a fresh discovery when Phase 7 starts.

**Follow-up investigation and fix, 2026-08-16:** root-caused with a diagnostic FFT test (`PitchShifterTests.cpp`, now promoted to a real assertion — "Spectral sidebands around the shifted fundamental stay below the measured WSOLA-fix thresholds"). `PitchShifter` calls itself "SOLA-style" but never actually performed SOLA's defining step: grains always relaunched at a fixed, phase-blind `baseDelaySamples` (`primaryNextSlot`/`quadratureNextSlot`'s schedule), with no correlation search to align a new grain's phase against the grain it crossfades with. Measured pre-fix: the strongest spurious spectral sideband, sitting almost exactly one grain-hop-rate (~55.5Hz at 44.1kHz) below the shifted fundamental, was -92.24dB at 0st (noise floor, no real artifact), -28.33dB at +7st, and **-19.99dB (~10% amplitude) at the default +12st** — this periodic amplitude modulation at the hop rate is what reads as "metallic/robotic".

Fix: added a WSOLA-style per-grain phase-alignment search, `PitchShifter::findAlignmentOffset()` (see `PitchShifter.h/.cpp`). At each grain's launch, it extrapolates the outgoing (crossfading-out) grain's trajectory from the shared delay line and searches candidate launch offsets for the one whose own extrapolated trajectory best cross-correlates (normalized) with the outgoing grain's, over the actual crossfade/overlap window; the found offset is stored per-grain (`Grain::baseDelayOffset`) rather than as a class-wide constant.

Two real bugs surfaced and were fixed during implementation, not just the base idea:
1. **Extrapolation rate.** The first attempt reused `grainDelaySamples`'s per-real-elapsed-sample rate, `(pitchRatio - 1.0f)`, inside the search — but the search reads multiple virtual future instants from a single frozen delay-line snapshot (no real per-sample push/pop advancing between them), which needs rate `pitchRatio`, not `pitchRatio - 1.0f` (the "-1" term is time elapsing for free in the real per-sample loop, which isn't available here). The wrong rate degenerated completely at 0 semitones (`pitchRatio - 1.0f == 0`, so every sample in the search window read the identical instant) and made the 0st sideband **worse** (0.22dB, i.e. louder than the fundamental) instead of better — caught by re-running the diagnostic test after the first attempt rather than assuming success.
2. **Search radius too small.** The initial radius (`crossfadeSamplesInt`, ~88 samples/2ms) was too small to track the alignment offset's actual drift: because the fixed hop schedule isn't commensurate with the signal's period, the true optimal offset drifts by several samples every hop and saturates at the search boundary after only ~11 hops, after which a pinned, effectively-constant offset can no longer track a continuously drifting phase error — which is why the first (rate-corrected) attempt still barely moved the +7st/+12st sideband levels. Widening the radius to a full grain length (`grainLengthSamplesInt`, ~882 samples/20ms) fixed this.

Measured after both fixes (same diagnostic signal/config): **0st unchanged at -92.24dB** (correctly untouched — nothing to fix at unity ratio), **+7st: -67.07dB** (~38.7dB improvement), **+12st: -70.60dB** (~50.6dB improvement, comfortably past the "-30dB or better" bar set for closing this investigation), **+24st: -38.17dB** (pre-fix this case was unmeasurable — the theoretical fundamental bin landed in a spectral null caused by the artifact itself, magnitude ~0.14 vs a healthy ~8156 post-fix; the peak-search was also hardened to find the actual nearest local-maximum bin within a small window instead of trusting the theoretical index outright, so this case no longer silently reports a meaningless ratio if a similar null recurs). All existing PitchShifterTests/ShimmerReverbEngineTests continued to pass throughout (22/22); the pitch-accuracy zero-crossing sweep's errors also dropped closer to 0% as an incidental side effect of phase-aligned splices reducing edge jitter.

**Status: numerically closed, by-ear confirmation still open.** This environment cannot listen to audio, so the remaining step — confirming the "metallic/robotic" character is actually gone by ear, at the default +12st shift and across the parameter range called out in this phase's Definition of Done (extreme feedback + extreme shift combinations run for several minutes) — is still pending and should happen as part of Phase 7's ear-validation pass rather than being assumed from the spectral measurement alone.

**Performance-regression follow-up, 2026-08-16:** the metallic/robotic fix above was confirmed correct by ear in the Standalone, but its search radius (`alignmentSearchRadiusSamples = grainLengthSamplesInt`, ~882 samples at 44.1kHz) made `findAlignmentOffset()` brute-force-correlate `(2*882+1) * crossfadeSamplesInt` (~88) Lagrange3rd-interpolated delay-line reads at *every* grain launch, in *both* the primary and quadrature pools, roughly every ~794 samples (~18ms) -- i.e. constantly. A wall-clock diagnostic added to `PitchShifterTests.cpp` ("DIAGNOSTIC: wall-clock real-time factor...") measured a single, fully-isolated `PitchShifter` instance (no tank, no other DSP, no host overhead) at only **1.19x real-time** in this Debug build -- almost no margin once inside `ShimmerReverbEngine`'s actual feedback loop alongside `DattorroTank`/DC blocker/limiter and real host/driver overhead. This was the root cause of the reported audio dropouts/interruptions in the Standalone.

Root cause: the search always re-derived the FULL cumulative phase drift from scratch around the fixed nominal `baseDelaySamples` origin at every launch, because the fixed hop schedule isn't commensurate with the input signal's period and the true optimal offset drifts continuously -- so the radius had to be a full grain length just to "catch up" to wherever that drift had wandered since the last launch.

First fix attempt (incremental/seeded search): centered the search on the *previous* grain's own found offset (`primaryLastOffset`/`quadratureLastOffset`, new members carried forward across launches) instead of the fixed origin, on the theory that each launch then only needs to cover that single hop's *incremental* drift, not the full cumulative drift -- letting the radius shrink dramatically. This worked well for +7st and +12st (small radii of 6-12 samples held their sideband levels within a few dB of the original brute-force numbers, at 40-75x real-time), but an exhaustive sweep of the radius from 1 up to 800 (i.e. matching the original design's full search width) showed the +24 semitone case (pitchRatio=4.0) never got anywhere close to the required -30dB sideband threshold -- it plateaued somewhere between -8dB and -23dB depending on radius, because once the previousOffset-seeded search wandered to a bad self-consistent region it had no way back to the actually-best (zero-anchored) alignment; a decay-toward-zero variant was also tried and made things *worse* (it broke the otherwise-excellent +7st/+12st tracking without fixing +24st), so that avenue was abandoned too.

Final fix (two-anchor search): `findAlignmentOffset()` now searches two small windows every call -- one around the running `previousOffset` (cheap continuous-drift tracking, as above) and one around the fixed nominal zero (re-checked every single call, guaranteeing the search can never permanently drift away from the known-good absolute alignment). Both windows use the same small radius, so the total cost is still tiny next to the original single full-grain-length search. Tuned empirically: `alignmentSearchRadiusSamples = juce::jmax(16, crossfadeSamplesInt / 4)` (22 samples at 44.1kHz, where `crossfadeSamplesInt = 88`) is the smallest radius that clears the +24st threshold with headroom; the improvement saturates by radius=24 (-54.2dB) so 22 was chosen just below that plateau rather than paying for extra, no-longer-helpful width.

Measured before/after this follow-up (same diagnostic signal/config as the original fix above):
- **Spectral sidebands** (relative to fundamental): 0st unchanged at -92.24dB; +7st -67.07dB &rarr; **-59.19dB** (threshold -55dB, ~4dB margin); +12st -70.60dB &rarr; **-61.00dB** (threshold -50dB, ~11dB margin); +24st -38.17dB &rarr; **-44.67dB** (threshold -30dB, ~14.7dB margin, actually *better* than the original brute-force number).
- **Real-time factor** (same isolated-`PitchShifter`, 5s-of-audio wall-clock measurement): **1.19x &rarr; ~19.3x** (a ~16x improvement), now asserted in the DIAGNOSTIC test with an 8x regression floor (previously log-only).
- Full suite: **23/23 tests passed**, no reduction in coverage.

Files touched: `Source/DSP/PitchShifter.h` (new `primaryLastOffset`/`quadratureLastOffset` members, `findAlignmentOffset()` signature/comments), `Source/DSP/PitchShifter.cpp` (`prepare()`'s radius formula, `reset()`'s offset rewind, `findAlignmentOffset()`'s two-anchor search body, both `processSample()` launch call sites), `Tests/Source/PitchShifterTests.cpp` (updated sideband-test comment with the new measured numbers, added the real-time-factor regression assertion).

### Phase 8 — Shimmer character alignment (new, added 2026-08-16)

**Goal**: after Phase 7's performance fix, the user confirmed the audio runs smoothly (no more dropouts) but reported the wet signal "doesn't sound at all like a shimmer reverb" — it reads as a plain-ish plate reverb, not the ascending pitched wash the genre is known for (Valhalla Shimmer and the original Eno/Lanois technique this project's own `shimmer-reverb-concepts.md` cites as the reason Dattorro was chosen in the first place — see that doc's line "We're using the Dattorro topology because real shimmer plugins (Valhalla Shimmer and others) are built on it"). This phase closes that gap without reopening Phase 3/4's already-validated stability work.

A `codegraph_explore`-traced comparison against the widely-documented canonical shimmer pattern (Valhalla's own source is closed; this is the general design Sean Costello and the DSP community have described, and what `shimmer-reverb-concepts.md`/`shimmer-reverb-architecture.md` already cite) found:

- **Matches, already correct**: the shifter reads `DattorroTank::peekFeedbackSignal()` (the tank's own recirculating tail, not the dry input) and feeds the result back into the tank's cross-feed junction (`ShimmerReverbEngine::process()` &rarr; `tank.processSample(monoData[i], safeFeedback)`) — this is the one structural choice that actually defines "shimmer" (shift the tail, recirculate the shifted result so it cascades), and it's already right.
- **Real gap 1 — no independent shimmer/decay control.** `DattorroTank::processSample()`'s `inputToA = diffused + decayGain * recirculatingFeedback` uses the SAME `decayGain` (the one `Feedback` APVTS parameter) for both the plain reverb's own cross-feed sustain (`inputToB`'s `decayGain * tankA_out`) and the shifted-feedback path. This was a **deliberate Phase 3 fix**, not an oversight (see Phase 3's note in "Current state" above: "decayGain is now the single knob controlling both sustain and shimmer amount" — it replaced an earlier, structurally-inaudible separate/weak parallel shimmer loop). Valhalla Shimmer and canonical designs expose these as two independent controls (long plain decay with little shimmer, or a short tail with a huge shimmer wash); ours can't do that today.
- **Real gap 2 — `Width` is a mislabeled dual-purpose knob, AND its direct-injection term is the wrong mechanism for "shimmer presence."** `ShimmerReverbEngine::process()`'s `wetLeft = tankOut + shimmerWidthGain * safeFeedback` / `wetRight = tankOut + shimmerWidthGain * quadratureSafe` inject the (post-DC-block, post-limiter) shifted signal **directly** into the wet output — a single-generation, essentially undiffused copy of one pass through the shifter — on top of whatever `tankOut` already carries. `shimmerWidthGain` (`Width`, default `0.3f`) is simultaneously "how much of that raw copy reaches the output" AND "how decorrelated L/R are."

  **Ear-test result, 2026-08-16 (`Feedback` near 0.85, `Width` at 1.0, default +12st): confirmed this is actively harmful, not just mislabeled.** User's report: "sounds like if there is a pitch shift in parallel to the reverb... chipmunk effect... not really harmonic, lush or ethereal." This is the direct-injection term dominating the mix once turned up — at `Width = 1.0` a raw, minimally-diffused single-pass copy is added at full strength, and a single clean pitch-shifted copy summed with a reverb reads exactly as "a parallel pitch shifter bolted onto a reverb," not as shimmer. **Self-correction: my own suggested zero-code test (`Width` &rarr; 1.0) is what exposed this — it was diagnostically useful (it's why we know the direct-injection path is the problem) but is not itself a fix; don't ship `Width` anywhere near 1.0 as a "solution."**

  The actual lushness of a real shimmer comes from `tankOut` itself, not from this direct term: because the shifter sits *inside* the recirculating loop (Real gap 1's context), each pass through `tank.processSample()` sends the shifted signal through the FULL branchA &rarr; branchB diffusion chain (allpass/delay/damping/allpass/delay each), and it gets tapped and shifted *again* the next time `peekFeedbackSignal()` is read — so `tankOut` should already contain many generations of increasingly-shifted, increasingly time-diffused content summing together, which is what should read as a dense, blended, "cloud-like" wash rather than one clean layer. The direct `shimmerWidthGain * safeFeedback` term bypasses all of that diffusion for its own contribution, so turning it up doesn't add more lushness — it adds more "clean parallel pitch shifter," competing with (and masking) the properly-diffused cascade that's already baked into `tankOut`.

- **Not actually missing (self-correction from the first pass of this investigation)**: `DattorroTank`'s per-branch `dampingCoefficient` (`processBranch()`) already runs on every recirculating sample, including shifted content that has re-entered via branch A — so the shifted path is NOT undamped/unfiltered as first claimed in this session's chat discussion. The real nuance is narrower: it's ONE coefficient serving both the plain tail's character and the cascading-pitch-shifted content's brightness, with no way to tune them separately — a smaller gap than "no filtering at all," and only worth addressing if the ear pass below still finds it necessary.

Tasks:
- [x] Zero-code sanity check: re-listened with `Feedback` near 0.85 and `Width` at 1.0 — **result: worse, not better** ("chipmunk" effect, see above). This ruled out "just turn the existing knobs up" as a fix and pointed at the direct-injection term specifically, which the tasks below now address; it does not mean Real gap 1/2 aren't real, it refines what the actual fix has to be.
- [x] Decouple shimmer sustain from tank decay: add a new `shimmerAmount` parameter (0..1, default `1.0f` for exact backward compatibility) that multiplies `safeFeedback` in `ShimmerReverbEngine::process()` right before `tank.processSample()` is called, so the total gain reaching the tank's A-branch cross-feed becomes `decayGain * shimmerAmount` instead of `decayGain` alone. This is now understood to be the PRIMARY lushness control — it scales how much shifted content ends up cascading through `tankOut`'s own diffusion, not a raw parallel copy — so this task matters more than originally framed. Capping it to `[0, 1]` (never amplifying) means it can only ever reduce the shimmer's contribution relative to today's behavior — the Phase 4 runaway-safety margin (`decayGain < 0.85`) is never exceeded, so that work does not need re-verifying; **flag for the ear pass**: if `shimmerAmount` at its max (1.0, i.e. today's behavior) still doesn't sustain long/loud enough for the cascading generations to overlap into a dense wash before decaying out, a *separate*, explicitly-decided follow-up (allowing this specific path some headroom above unity, leaning on the existing `SafetyLimiter`/`DCBlocker` already in that path) may be needed — don't silently assume `[0,1]` is sufficient range without checking by ear first. — **Done 2026-08-18**: `ShimmerReverbEngine::setShimmerAmount()` added, clamped `[0,1]`, wired through APVTS (`ParamIDs::shimmerAmount`, default `1.0f`) and `PluginProcessor`'s smoothing exactly like `Feedback`. Verified bit-for-bit identical output at the default vs. an untouched engine (new regression test), and finite/bounded at both extremes (`0.0f`, and `5.0f` clamping to `1.0f`). Ear pass on the sustain-length flag above is still open.
- [x] Rework the direct-injection term instead of just relabeling it: reduce `shimmerWidthGain`'s practical weight/default significantly (its honest job is subtle stereo-decorrelation seasoning, not carrying the shimmer's presence — that's `tankOut`'s job), and/or restructure it to inject only the *difference* between the primary and quadrature shifted signals for width, rather than the full raw signal — so it stops competing with/masking the properly-diffused cascade. Rename to something honest once its role is narrowed (e.g. keep `Width` meaning decorrelation only). — **Done 2026-08-18**: `wetLeft`/`wetRight` now inject only `shimmerWidthGain * 0.5f * (safeFeedback - quadratureSafe)` (a pure side/decorrelation term) instead of a full raw copy of `safeFeedback`/`quadratureSafe` per channel; at `Width = 0` this collapses to `wetLeft == wetRight == tankOut` exactly, same as before. Default lowered `0.3f → 0.15f` (in both `ShimmerReverbEngine::defaultShimmerWidthGain` and the APVTS `Width` parameter default) — a reasoned starting point, not yet ear-tuned. Kept the `Width` name since its role is now honestly "decorrelation only."
- [ ] Only if the ear pass after the above still finds the shifted content too harsh/thin: add a shimmer-path-specific damping stage (e.g. a one-pole low-pass on `shiftedFeedback` before it re-enters the tank, inside `ShimmerReverbEngine::process()`) independent of `DattorroTank`'s own `dampingCoefficient` — don't add this preemptively given the self-correction above. — not attempted; explicitly conditional on the ear pass below, which hasn't happened yet.
- [ ] Ear-validate specifically against the original complaint, with `Width` kept LOW/near default this time (not cranked, per the corrected understanding above): at `shimmerAmount = 1.0f` (everything else unchanged) output must sound identical to today's engine; then, moving `shimmerAmount` and `Feedback` independently, confirm the wet signal reads as a lush, ascending, blended pitched wash — explicitly distinguishing this result from the "chipmunk"/parallel-pitch-shifter artifact produced by raising `Width` instead. — **not done**: this environment cannot listen to audio; needs the user's ears in the Standalone.
- [ ] Update `shimmer-reverb-concepts.md`/`shimmer-reverb-architecture.md`'s "Feedback gain" row and Phase 3's "single knob" language once the new parameter lands, so later phases don't cite the old decision as still current. — **`shimmer-reverb-architecture.md` no longer exists as a file in `docs/`** (superseded/merged at some earlier point) and `shimmer-reverb-concepts.md` has no "Feedback gain" row to update; this "Current state" section's own Phase 3 note above still says decayGain is "the single knob" and should be read as superseded by this Phase 8 entry instead.
- [x] **UI**: `shimmerAmount` is a new user-facing creative control, not an internal-only constant — add a matching rotary knob in `PluginEditor` (same pattern as the existing five: `juce::Slider` + `Label` + `AudioProcessorValueTreeState::SliderAttachment` against `apvts`, no direct engine/processor calls from the editor, per the `juce-plugin-dev` hard rule). This ships in the SAME phase as the DSP parameter, unlike the original Phase 5/6 split — that split made sense for the initial from-scratch parameter set, but shipping a new DSP knob with no way to turn it would leave Phase 8 half-finished. — **Done 2026-08-18**: `shimmerAmountSlider`/`shimmerAmountLabel` added via `configureRotary()`, attached through `shimmerAmountAttachment` (`SliderAttachment` against `apvts`/`ParamIDs::shimmerAmount`), placed in the same `FlexBox` knob row as the existing five. No direct processor/DSP calls from the editor.

**Structural follow-up, 2026-08-18 (same day, after the ear pass above):** the gain-staging fix (tasks 303/304) did NOT fix the actual complaint — user re-listened and reported it "still sounds like a chipmunk and also a bit metallic," not like a shimmer reverb. Root-caused by comparing this project's own design research notes (`docs/reverb-design-research-notes.md`, `docs/shimmer-reverb-open-source-survey.md`, `docs/reference/lushverb-shimmer-reverb.md`) against the actual implementation, rather than retuning parameters again:

- **Real root cause**: `ShimmerReverbEngine` had only ONE recirculation path into `DattorroTank` — `inputToA = diffused + decayGain * recirculatingFeedback`, where `recirculatingFeedback` WAS the externally pitch-shifted signal, entirely REPLACING the tank's own natural `feedbackFromB` term. Every lap re-shifted an already-shifted signal: gen1 +12st, gen2 (reshifting gen1) +24st, gen3 +36st — geometric pitch compounding that leaves the musical register in 2-3 generations. That reads as one clean transposition ("chipmunk"), not a blended cascading wash — this is task 303's "Real gap 1" (decayGain doing double duty), not actually fixed by shimmerAmount alone, since shimmerAmount only scaled that one loop's gain without decoupling "does this pass get reshifted again."
- **Fix**: `DattorroTank::processSample()`'s cross-feed sum changed to `inputToA = diffused + decayGain * feedbackFromB + shimmerFeedbackGain * externalFeedback` — the tank's own natural recirculation is now ALWAYS present (a genuine, persistent plain decay, exactly Phase 1/2/3's already-proven-stable topology), and the externally-shifted signal is a SEPARATE, independently-gained ADDITIVE term layered on top (new `DattorroTank::setShimmerFeedbackGain()`, backed by the same `shimmerAmount` parameter — `ShimmerReverbEngine::setShimmerAmount()` now forwards to it instead of pre-scaling `safeFeedback` itself). This lets the shimmer wash build as a genuine cascading stack of octave-spaced layers (each pass reshifts whatever is CURRENTLY in the tank, which already contains all previous layers) while the plain tail keeps sustaining independently underneath, governed purely by `decayGain`.
- **Verified**: a new regression test proves `shimmerAmount = 0.0` makes `ShimmerReverbEngine` (with `Width` also zeroed) produce bit-for-bit identical output to a bare `DattorroTank` fed the same input — hard proof the two lanes are genuinely decoupled, not just differently tuned. The decay-to-silence test's threshold needed recalibrating (measured, not guessed): the plain tail's own persistent decay now takes longer to cross the 1e-3 silence threshold (~35.9s, vs. the old fully-shift-routed path's ~9s) since it's a real, continuously-decaying exponential tail (peak-per-second trace strictly monotonic the whole way) rather than self-oscillation — `silenceSeconds` widened `12.0 → 40.0` with the measured numbers documented in the test's own comment. All 30 `MilleniaTests` pass (29 + this new one); `DattorroTankTests.cpp` untouched and still green. `decayGain`/`dampingCoefficient` were NOT changed to force any test to pass.
- **Status (superseded by the correction below, kept for history)**: numerically/structurally verified at the time, but the ear pass that followed found a real bug in this first attempt.

**Structural follow-up, second pass, 2026-08-18 (same day):** user tried the fix above and reported two new, worse symptoms instead of "chipmunk": at high `Feedback` + high `Shimmer Amount`, a wall of high-frequency noise rising in volume and covering everything; at high `Feedback` + low `Shimmer Amount`, "oscillating, very unnatural, even psychedelic." Both traced to a real bug in the first pass's additive formula, not a new gain-staging retune:

- **Real root cause**: `inputToA = diffused + decayGain*feedbackFromB + shimmerFeedbackGain*externalFeedback` ADDS two independent gain terms into the same recirculating sum. Since `externalFeedback` is itself a near-unity-gain function of `feedbackFromB` (shifted, DC-blocked, limited), the EFFECTIVE combined loop gain is closer to `decayGain + shimmerFeedbackGain`, not `decayGain` alone — ~1.7 at defaults (0.7 + 1.0), and still >1.0 even at "high Feedback (0.85), low Shimmer Amount (0.2)" (~1.05). A loop gain past unity only stays technically bounded because `SafetyLimiter`'s hard clipping catches it every cycle — which is exactly "oscillating/psychedelic," and at high pitch shift each generation's content pushes toward Nyquist where nothing in the loop tames it, producing the rising wall of noise (at low/negative shift, the DC blocker's own highpass action bleeds energy out of downward-shifted content instead, which is why that case just sounds like a bounded "rumble" rather than escalating). The automated boundedness test didn't catch this because a limiter-clipped signal is still finite and ≤10.0 — the test checks safety, not character.
- **Fix**: replaced the additive sum with a budget-conserving crossfade: `shimmerWeight = shimmerFeedbackGain * maxShimmerBlendWeight` (new constant, `0.5f`), `plainWeight = 1.0f - shimmerWeight`, `inputToA = diffused + decayGain * (plainWeight*feedbackFromB + shimmerWeight*externalFeedback)`. Since `plainWeight + shimmerWeight` is always exactly `1.0`, the combined magnitude can never exceed `max(|feedbackFromB|, |externalFeedback|)` — total loop gain is rigorously bounded by `decayGain` alone (the same `<=0.85` ceiling Phase 4 already validated), for ANY `shimmerAmount` setting. Capping `shimmerWeight` at `0.5` (not `1.0`) also guarantees at least half the recirculating budget always stays on the plain/unshifted path even at max shimmer, preventing the original geometric-pitch-compounding ("chipmunk") bug from reappearing at the top end.
- **A second bug surfaced during verification, already fixed**: the bare-`DattorroTank` regression test (added in the first pass) never called `setShimmerFeedbackGain(0.0f)` on its comparison tank, so it silently ran at the class default (`1.0`) — harmless under the old additive formula (multiplied by an always-zero `externalFeedback`) but not under the new crossfade formula, where `shimmerFeedbackGain` affects `plainWeight` regardless of what `externalFeedback` is. Fixed with one explicit `setShimmerFeedbackGain(0.0f)` call in the test; this was a test-construction bug, not a DSP bug.
- **Verified**: all 30 `MilleniaTests` pass, including the corrected bare-tank-equivalence test. The decay-to-silence test passed as-is with its existing `40.0s` window (from the first pass) — no further timing recalibration needed, since that window already had margin. `decayGain`, `maxShimmerBlendWeight`, and `shimmerFeedbackGain`'s default were not tuned to force any test to pass.
- **Status**: numerically/structurally verified (twice now — once wrongly, once corrected with a rigorous gain-budget proof), by-ear confirmation still open. This is the actual point where the ear pass below should happen next.

**Third pass, 2026-08-21 — Width leak bug, found and fixed by ear.** User ear-tested the crossfade fix above and reported it "still sounds bad: noisy, chipmunky, low-bitrate... shimmer disappears/changes quite fast... even when Mix and Feedback is at 100%," plus a concrete, isolatable bug: turning `Shimmer Amount` fully down (`0.0f`) did not silence the shimmer character.

- **Root cause**: `ShimmerReverbEngine::process()`'s Width/decorrelation term (`sideShift = safeFeedback - quadratureSafe`, injected into `wetLeft`/`wetRight`) reads the pitch shifter's output directly and runs every sample regardless of `shimmerAmount` — `shimmerAmount` only ever gated `DattorroTank`'s own internal recirculation crossfade, never this separate feed-forward term. The existing bare-tank-equivalence regression test masked this exact bug because it always zeroed BOTH `shimmerAmount` AND `Width` together.
- **Fix, TDD**: added a new regression test (`shimmerAmount=0.0, Width=1.0` &rarr; L must equal R) that reproduced the bug first (88816/102400 sample-assertions failed), then fixed by storing `shimmerAmount` locally in `ShimmerReverbEngine` (previously the class forwarded it to `DattorroTank` and kept nothing) and multiplying `sideShift`'s contribution by it too. Full suite green (31/31) after the fix.
- **Confirmed by ear**: user re-tested and confirmed "Shimmer Amount now works as expected, applying no shimmer when amount is 0." This part of Phase 8 is closed.
- **New ear-test findings, same pass**: (1) sounds "way better, smoother" overall with the leak fixed, confirming the leak was a real contributor to the harsh/noisy character, not just the amount=0 case; (2) `Width` still sounds worse the higher it's turned up — expected (it's still a raw difference-signal artifact at high settings, hence its conservative `0.15f` default), not a new bug, deprioritized; (3) **the shimmer still decays/finishes too soon vs. commercial shimmer reverbs even with Feedback/Shimmer Amount/Mix all at maximum** — traced to `maxShimmerBlendWeight`'s `0.5f` cap (see the second-pass note above): at `decayGain=0.7`, the shifted lineage's effective per-lap gain was at most `~0.35`, inaudible after ~2-3 tank round-trips (~2s) vs. the plain tail's ~35s; (4) **negative (downward) pitch shift's shimmer is barely noticeable** — not yet investigated, no confirmed root cause; DCBlocker's fixed 20Hz highpass and/or simple perceptual masking (downward-shifted content blending into the existing bass/mid register rather than standing out the way upward shifts do into a brighter, less-occupied register) are candidate hypotheses, neither verified. Flagged for a future session, kept separate from the sustain-length issue since it may have an unrelated cause.
- **Fix for finding (3), 2026-08-21**: raised `maxShimmerBlendWeight` `0.5f -> 0.85f` (mirrors `decayGain`'s own validated-safe ceiling). The crossfade's gain-safety property (`plainWeight + shimmerWeight == 1.0` exactly) holds for any cap up to `1.0`, not just `0.5` — the cap's real job is only reserving *some* budget for the plain path against full geometric pitch-compounding at `shimmerAmount`'s maximum, not gain safety itself, so raising it does not reopen the earlier additive-gain instability bug. Full suite still green (31/31), including the `40.0s` decay-to-silence window unchanged. **Confirmed by ear**: user reported the sustain is "much longer now."

**Phase 8 CLOSED, 2026-08-21.** User's own words closing it out: "sounds much longer now, still needs some tuning but we can handle it in next phase." The shimmer character is now structurally correct and ear-confirmed as a real improvement (Width leak fixed, shimmerAmount=0 genuinely silent, sustain meaningfully longer) — remaining items are acknowledged, ear-tuning-only follow-ups, deliberately deferred rather than blocking this phase's close:
- `Width`'s character at higher settings (still a raw difference-signal artifact, unresolved from the third-pass notes above).
- Negative/downward pitch shift's shimmer being barely noticeable (finding (4) above, not yet investigated at all).
- Any further by-ear tuning of `maxShimmerBlendWeight`/`decayGain`/`shimmerWidthGain` now that the structure itself is correct.
These three are carried forward as the start of the next phase's work, not new Phase 8 scope.

Definition of done:
- [x] `Feedback` (decay) and `shimmerAmount` move independently and each produces the expected, distinct audible change. — structurally verified (bit-exact bare-tank cross-check) AND ear-confirmed: user reported `shimmerAmount=0` is genuinely silent and the longer sustain at `0.85` blend weight is clearly audible.
- [x] All new parameters at their backward-compatible defaults reproduce perceptually identical output to the current engine — this phase must not silently change the sound for anyone who doesn't touch the new controls. — verified bit-for-bit (not just "perceptually") for `shimmerAmount` at its default via a dedicated regression test comparing an untouched engine against one with `setShimmerAmount(1.0f)` explicitly called.
- [x] A live ear pass confirms the shimmer character is clearly recognizable as a lush, blended, ascending wash (NOT the "chipmunk"/parallel-pitch-shifter artifact this ear test found) at the default +12st pitch shift. — user confirmed "way better, smoother" after the Width-leak fix and a longer sustain after the `0.85` blend-weight change; user explicitly chose to close here rather than hold the phase open for further tuning or a reference-plugin A/B.
- [x] All existing DSP unit tests continue to pass; new parameters get the same coverage bar Phase 5 set (state round-trip, bounded/finite output at extremes). — 31/31 `MilleniaTests` pass, including 4 new `ShimmerReverbEngineTests` cases (default-equivalence, `0.0f` boundedness, `5.0f`-clamps-to-`1.0f` boundedness, Width-leak regression). APVTS-level state round-trip remains untestable in this project's test target (documented pre-existing gap — the console test app doesn't link `juce_audio_processors`).
- [x] The new `shimmerAmount` knob is visible, labeled, and wired through an APVTS attachment in the editor — automating it from a host produces the same audible change as turning the knob. — wiring verified in code/build and confirmed by ear (turning it to 0 audibly silences the shimmer).

Pitfalls: keep `shimmerAmount` capped at `[0, 1]` (never a multiplier above unity) so the combined `decayGain * shimmerAmount` gain can never exceed what Phase 4 already validated as stable, UNLESS the ear pass explicitly finds this insufficient — in which case treat any headroom-above-unity change as its own deliberate, documented decision, not a silent range bump; treat this phase as a deliberate evolution of Phase 3's "single knob" decision (made to fix a real, different bug — a too-weak separate parallel loop), not a reversal that reintroduces it, since the shifter still sits inside the tank's one recirculation path either way; do NOT use `Width`/direct-injection level as a substitute for shimmer presence — this ear test proved that reads as a parallel effect, not blended shimmer.

### Phase 9 — Freeze / drone capture and pitch-shift preset completion (new, added 2026-08-16)

**Carried over from Phase 8's close (2026-08-21), do first:** three ear-tuning-only follow-ups deliberately deferred rather than blocking Phase 8's close — `Width`'s character at higher settings (still a raw difference-signal artifact), negative/downward pitch shift's shimmer being barely noticeable, and any further by-ear tuning of `maxShimmerBlendWeight`/`decayGain`/`shimmerWidthGain` now that Phase 8's structure is confirmed correct. See Phase 8's closing note above for full context.

**Negative pitch shift investigated, 2026-08-21 — hypothesis disproved, root cause NOT structural.** Suspected the shifter sitting inside the tank's own recirculation loop (`ShimmerReverbEngine::process()`: `shiftedFeedback = shifter.processSample(tank.peekFeedbackSignal())`, fed back via `DattorroTank::processSample(input, externalFeedback)`) would let a downward cascade (ratio < 1) run into `feedbackDcBlocker`'s fixed 20Hz one-pole highpass (`DCBlocker.h`) — which sits in that same loop — far sooner than an upward cascade runs out of headroom toward Nyquist, since a typical fundamental has only 1-2 octaves below it to 20Hz vs. several above it. Added a permanent diagnostic regression test (`ShimmerReverbEngineTests.cpp`, "DIAGNOSTIC: downward pitch-shift cascades die into DCBlocker's subsonic cutoff...") driving the engine with a matched-seed noise burst at +-12st and +-24st, then measuring bucketed per-second RMS over a 40s silent tail. Measured (Debug build, `MilleniaTests.exe`): +12st stayed above -60dB (of its own peak) for 7/40s, -12st for 7/40s (identical); +24st for 7/40s, -24st for 6/40s (1s difference); first-second-of-silence RMS was near-identical between directions at both magnitudes (e.g. 0.1558 up vs. 0.1567 down at +-12st). This **disproves** the DC-blocker-cutoff hypothesis for broadband-noise input — no meaningful absolute-level or tail-length asymmetry exists at the DSP/RMS level, so `DCBlocker`'s cutoff was deliberately left unchanged (20Hz) rather than speculatively tuned against un-confirmed evidence. Full `MilleniaTests` suite still passes (0 failures) with the new test added.

Left open, genuinely needs an ear pass (not reproducible with the broadband-noise/RMS methodology available without an audio device this session): the complaint may be psychoacoustic (equal-loudness contours make an objectively-equal-RMS low-frequency shimmer layer sound subjectively quieter than a high-frequency one to human hearing) rather than a DSP amplitude bug, or it may only manifest with tonal/musical input rather than noise, or with the shifter's actual grain-boundary artifacts at low pitch ratios interacting differently with real program material. Next step once the user can listen again: A/B +12st vs -12st by ear at matched Feedback/Shimmer Amount/Mix and describe what "barely noticeable" actually sounds like (thinner? shorter? just quieter?) so a targeted fix (if any) can be scoped instead of guessed at.

**Goal**: two features requested alongside Phase 8 but deliberately split out because they're a different kind of work (new capability, not a bug fix for the existing shimmer character): (1) a **Freeze** control that turns the currently-playing sound into a sustained drone; (2) the two missing pitch-shift quick-select presets (0st, −12st) from the original UI mock, alongside the existing +7/+12/+19 (Phase 6 shipped without them — see that phase's status note, "doesn't exactly match an earlier visual mock," a known, explicitly-carried-forward gap, not new scope).

**Freeze mechanism — decided 2026-08-16: option (a), classic infinite-sustain trick.** Two mechanisms were weighed:
- **(a) [CHOSEN]** Pin `DattorroTank`'s `decayGain` to unity (or just under, e.g. `0.999f`, to sidestep an exact-1.0 edge case) and mute the fresh dry input into the diffuser while frozen, so the tank's EXISTING tail — including whatever shimmer was already cascading — just recirculates forever without decaying or being disturbed by new input. Cheap: no new buffer, no buffer-length decision, reuses the tank that already exists. The tank's *live, still-evolving* recirculating content is what gets frozen (not a perfectly static single-loop drone) — user's call: "I think it will be enough," so this is the committed approach for Phase 9, not left open. This also makes the drone-length question moot: there's no fixed capture window at all under this mechanism, the sustain is simply continuous for as long as Freeze stays engaged.
- **(b) [NOT PURSUED for now]** True buffer capture-and-loop (record a fixed window, play it back on a hard loop). Materially more DSP work (pre-sized real-time-safe buffer, an explicit length decision, crossfaded loop-point splice) for a perfectly static drone. Left here as a documented alternative in case (a) turns out insufficient by ear once built — not deleted, just not the current plan.

Tasks:
- [x] Expose Freeze as a `juce::AudioParameterBool` routed through APVTS (per `juce-plugin-dev`'s hard rule — not a processor-only flag invisible to automation/state save). — **Done 2026-08-21**: `ParamIDs::freeze` (`Parameters.h`) + `AudioParameterBool` in `createParameterLayout()` (`Parameters.cpp`), default `false`.
- [x] Mute the fresh dry input path into `DattorroTank` while frozen and pin `decayGain` near unity (e.g. `0.999f`) for the duration. — **Done**: `DattorroTank::setFreezeAmount()`/`frozenDecayGain = 0.999f` compute `effectiveDecayGain = decayGain + freezeAmount * (frozenDecayGain - decayGain)` inside `processSample()`, replacing the two bare `decayGain *` uses on `inputToA`/`inputToB`. `ShimmerReverbEngine::process()` separately scales the fresh dry sample by `(1.0f - freezeAmount)` before it reaches `tank.processSample()` — both halves of the mechanism are needed together (pinning decay alone while still feeding fresh input would be a slow runaway; muting input alone without pinning decay would just let the existing tail decay normally).
- [x] Re-verify stability under this relaxed assumption specifically — Phase 4's existing tests assumed `decayGain < 1.0` strictly (max APVTS value 0.85) and never exercised sustained near-unity feedback, so this needs its own bounded-output-over-several-minutes test with Freeze engaged, not an assumption that old coverage already applies. — **Done**: `DattorroTankTests.cpp` adds a 3-minute (180s) sustained-noise finite/bounded test at `setFreezeAmount(1.0f)`, plus a burst-then-15s-silence test proving the frozen tail stays audibly sustained (peak > 1e-2 after 15s) while an equivalent non-frozen tank decays to near-silence (peak < 1e-3) over the same window — both passed. `ShimmerReverbEngineTests.cpp` adds a matching ~30s bounded/finite check at `setFreezeAmount(1.0f)` through the full engine chain (shifter, DC blockers, limiter, dry-mute), plus a default-equivalence regression (`setFreezeAmount(0.0f)` bit-identical to an untouched engine).
- [x] Add the two missing pitch-shift quick-select presets (0st, −12st) in `PluginEditor`, matching the existing +7/+12/+19 `TextButton` pattern — same underlying `pitchShift` parameter value, not new parameters or modes. — **Done**: `preset0Button`/`presetNeg12Button` added, same `onClick` → `pitchShiftSlider.setValue(...)` pattern, laid out in the same preset row (widened to 5 buttons).
- [x] Wire a Freeze toggle/button in `PluginEditor` via a `ButtonAttachment`, same pattern as the existing Bypass toggle — no direct processor/DSP calls from the editor. — **Done, then superseded 2026-08-22**: originally a `freezeButton`/`freezeAttachment` `ToggleButton` next to `bypassButton`. Replaced with a `freezeSlider`/`freezeAttachment` rotary dial (same `configureRotary`/`SliderAttachment` pattern as the other 6 knobs) so the user can dial in a partial freeze amount live over a sounding signal instead of only snapping between 0 and 1 — the underlying `freeze` APVTS parameter changed from `AudioParameterBool` to `AudioParameterFloat` (`Parameters.cpp`) to support this; `PluginProcessor`'s smoothing/wiring needed no changes since `freezeAmount` was already continuous throughout the DSP chain. Editor window widened 620→720px to fit the 7th knob.

Definition of done:
- [ ] Engaging Freeze produces a stable, indefinitely-sustained drone from whatever was playing at the moment of engagement, with no click at the moment of engagement. — **Numerically/structurally verified, not ear-verified.** `DattorroTankTests.cpp`'s freeze-sustain test proves the mechanism works (frozen tail stays orders of magnitude louder than a non-frozen tail after 15s of silence) and stays bounded over 3 minutes; the click-free engagement claim relies on `PluginProcessor`'s `smoothedFreeze` (same 50ms-ramp `juce::SmoothedValue` pattern as every other parameter) but this environment cannot listen to confirm no audible click at the actual moment of engagement.
- [ ] Disengaging Freeze returns to normal live processing (dry input un-muted, `decayGain` restored to its APVTS-driven value) without a click or dropout. — **Implementation-verified (builds clean, smoothing wired identically to engagement), not ear-verified** for the same reason as above — no audio device available in this session.
- [x] All five existing presets plus the two new ones (0st, −12st) set the same underlying `pitchShift` parameter value, not separate parameters. — confirmed by code: all seven preset buttons' `onClick` lambdas call `pitchShiftSlider.setValue(...)`, none touch a separate parameter/attachment.
- [ ] Freeze's parameter is covered by the same state-round-trip/bounded-output test bar Phase 5/8 established. — **partially covered. Bounded-output half done** (see task list above). **State-round-trip half not done**, for the same pre-existing, documented reason Phase 5/8 already carry forward: `Tests/MilleniaTests.jucer`'s console app doesn't link `juce_audio_processors`, so APVTS-level `setStateInformation(getStateInformation())` round-trip tests can't compile in this test target. Confirmed still true before writing this phase's tests, not assumed.
- [x] Bounded-output test with Freeze engaged for several minutes of sustained input shows no runaway/clipping beyond `SafetyLimiter`'s existing threshold. — `DattorroTankTests.cpp`'s 3-minute (180s) sustained-noise test at `setFreezeAmount(1.0f)` passed (peak stayed within the same `<= 10.0` safety bound every other boundedness test in this project uses); `ShimmerReverbEngineTests.cpp`'s ~30s equivalent through the full chain also passed.

**Status, 2026-08-21**: DSP/parameter/editor implementation complete and unit-tested (33/33 `MilleniaTests` pass, up from 31 — 4 new freeze-specific tests: 2 in `DattorroTankTests.cpp`, 2 in `ShimmerReverbEngineTests.cpp`). Both the `MilleniaTests` console runner and the main `Millenia.sln` (VST3 + Standalone, Debug/x64) build clean via MSBuild. Remaining open items are exactly the ones that need a human at an audio device (click-free engage/disengage) or a test-infra change out of this phase's scope (APVTS state round-trip) — neither silently assumed passing.

Pitfalls: the near-unity `decayGain` assumption while frozen is a genuine, different stability case from everything Phase 4 tested (which assumed strictly `< 1.0`) and needs its own explicit verification, not a borrowed assumption — addressed, see the freeze-specific tests above. Make sure muting the dry input and restoring `decayGain` on disengage are both click-free (the existing parameter-smoothing pattern from Phase 5 is the natural mechanism, not a hard instant switch) — implemented via `smoothedFreeze`, same convention as every other parameter, but not yet ear-confirmed click-free (see Definition of Done above).

**Ear-tested by the user, 2026-08-21 — option (a) doesn't match user intent, redesign deferred to Phase 10.** After a stale incremental-build artifact was ruled out (a clean MSBuild rebuild reproduced the same result), the user confirmed option (a)'s actual behavior matches what was built: the tank's *live, still-evolving* content keeps recirculating without decaying, but it keeps morphing (diffusion + the in-loop pitch shifter keep reshaping it), so it's heard as a sustained, evolving texture/drone rather than "the last sounding portion looped." What the user actually wants is option (b) from this section's original two-way decision — true buffer capture-and-loop, an exact repeating loop of a captured window — plus a new user-adjustable loop-length dial (not a fixed constant, per the original option (b) note's open question). Decision: keep the existing option (a) implementation in place as shipped (it's a real, tested, independently-useful sustain feature, not being reverted), and schedule the loop-capture redesign as its own future phase (see Phase 10 below) rather than reworking Phase 9 further. Mix already composes correctly with either mechanism — it's the engine's final dry/wet blend, untouched by `setFreezeAmount()`, so lowering it already fades frozen content toward dry today; this does not need to wait for Phase 10.

**Ever-morphing drone fixed without the loop-capture redesign, 2026-08-22.** Before scheduling Phase 10, the user asked whether the drone's "keeps morphing" character could be tamed by tuning the existing recirculation instead of building the buffer/loop mechanism below. Root cause: `ShimmerReverbEngine::process()` always re-runs up to `maxShimmerBlendWeight` (0.85) of the tank's own recirculating content through `PitchShifter` every pass; at Freeze's near-lossless `frozenDecayGain=0.999f`, that pitch-shift compounds indefinitely instead of settling — that compounding, not diffusion alone, was the actual mechanism behind "keeps morphing." Fix: `ShimmerReverbEngine` now crossfades the shifter's own pitch ratio toward unity (0 semitones) as `freezeAmount` goes 0→1 (new `pitchShiftSemitones` member, applied in `setPitchShiftSemitones()`/`setFreezeAmount()`/`prepare()`), so the shimmer-injected path becomes a plain unshifted delay tap once fully frozen instead of a cascading pitch shift. A first attempt instead crossfaded `DattorroTank::processSample()`'s `shimmerWeight`/`plainWeight` split itself toward `plainWeight=1.0` — that broke `DattorroTankTests`' 3-minute frozen-boundedness test (peak exceeded the 10.0 safety bound within ~1 minute), because that 0.85/0.15 split turns out to be load-bearing for stability at near-unity decay, not just a tunable blend; reverted in favor of the pitch-ratio crossfade, which leaves that split untouched. All 33 `MilleniaTests` pass; user confirmed by ear ("much better now, aligned with the idea"). **This resolves Phase 9's "doesn't match user intent" close above for the drone-character complaint specifically** — Phase 10's true buffer capture-and-loop remains a distinct, separately-valuable feature (a hard exact-repeat loop is a different character than a held/settled drone, however static) but is no longer blocking on this complaint; it stays open below as its own future capability, not a fix for a still-broken Phase 9.

**Full-strength Freeze's decay/oscillation ("motor") complaint, partially fixed 2026-08-22.** After the dial swap above, the user reported Freeze=1.0 "still finishes soon, oscillates too much, sometimes sounds like a motor" instead of ethereal/static. A permanent diagnostic (`ShimmerReverbEngineTests.cpp`, "Freeze = 1.0's actual sustained tail") measured this directly: -6.7dB decay over 15s at default settings. Isolated the cause by disabling one candidate at a time: NOT `frozenDecayGain` (0.999→0.9999 barely moved it), NOT `SafetyLimiter`, NOT `DCBlocker` individually — only zeroing the shimmer path's weight entirely dropped decay to -0.15dB. **Root cause: structural comb filtering**, not a lossy stage — `DattorroTank::processSample()` sums the tank's own feedback with a copy that detoured through `PitchShifter`'s own ~80ms internal delay line; summing two correlated copies of the same content at different delays is destructive interference, invisible at ordinary decay but dominant at Freeze's near-unity decay. The catch: the config that eliminates this (`plainWeight`=1.0) is the exact config already proven unstable in this file's own 3-minute freeze-boundedness test. Swept `frozenMaxShimmerBlendWeight` (a new freeze-time cap on the shimmer path's weight, crossfaded from the existing 0.85f cap, see `DattorroTank.h`'s comment for the full sweep table) against both regression tests: the relationship is NOT monotonic (0.2f-0.6f is actually WORSE than the unmodified 0.85f baseline; only below ~0.05f does it improve, sharply). Landed on **0.01f**: -1.94dB decay (down from -6.7dB), verified stable over a 10-minute extended run (not just the committed 3-minute one), with ~2x margin above the observed instability cliff at 0.005f. This is a genuine partial fix, not a full resolution — some decay/oscillation remains (a real, accepted trade-off against the alternative of full instability), and the user chose this path (sweep a safe middle ground) over starting Phase 10's buffer-capture redesign, per an explicit fork presented after the investigation. Ear-confirmed "a bit better, but not great" — further digging (tap-delay length variations, grain-vs-fixed-tap, a finer weight sweep down to 0.007f) found no additional improvement without reintroducing instability (0.007f in particular looked fine on the committed 3-minute test but failed a 10-minute extended run — an important finding about how sharp/deceptive this stability cliff is).

**Output-stage auto-leveler added, 2026-08-22, same day, at the user's suggestion.** User proposed compensating Freeze's remaining volume loss with an amplifier tied to the freeze amount. Implemented as `FreezeLeveler` (`Source/DSP/FreezeLeveler.h`): a feed-forward-only stage applied to the already-computed wet signal, strictly after the tank/shifter chain and before the dry/wet mix — deliberately NOT inside the recirculating loop, since compensating there would fight the shimmerWeight/plainWeight balance this phase just spent considerable effort proving fragile. Mechanism: two one-pole envelope followers (a fast, freeze-independent one and a reference one whose time constant crossfades from that same fast value up to 30s as freezeAmount approaches 1.0) produce a compensating gain (reference/live, clamped to [1.0, 4.0x/+12dB]) applied identically to both channels (from a mono-summed measurement, so it can't skew the Width stereo image). At freezeAmount=0.0f this is mathematically a no-op (verified bit-identical, all existing tests still pass). Brought the diagnostic's 15s decay from -1.94dB down to **-0.54dB** — a substantial further improvement. Explicitly bounded, not a full fix: compensation degrades on longer holds (measured ~-3.7dB at 60s in a one-off check) since the +12dB ceiling is intentional (avoids amplifying noise floor indefinitely on very long freezes) — a real, accepted limitation, not a bug. Oscillation (~1.5dB) is unaffected by design (the leveler's slow smoothing doesn't touch short-term wobble). Ear-confirmed the volume side works well.

**Pitch-down side effect found and fixed, 2026-08-22, same day.** User reported "increasing freeze seems to produce a pitch down change that wasn't noticeable previously." Root cause verified in code, not assumed: the earlier pitch-cascade fix (`ShimmerReverbEngine`'s `pitchShiftSemitones * (1.0f - freezeAmount)` crossfade, see above) is LINEAR, so at freezeAmount=0.5 a +12st shift was already halved to +6st — a clearly audible change well before Freeze was anywhere near fully engaged, which the user correctly heard as "pitch down" (the shimmer's up-shifted layer fading out reads as the overall sound dropping in pitch). Fixed by raising `(1.0f - freezeAmount)` to a power (`pitchShiftCrossfadeCurve = 4.0f`, new `ShimmerReverbEngine::updateShifterRatio()` shared by `prepare()`/`setPitchShiftSemitones()`/`setFreezeAmount()`) instead of using it linearly: at freezeAmount=0.5 a +12st shift is now only reduced to ~+11.25st (a 6% cut, essentially inaudible), while still reaching EXACTLY 0 semitones at freezeAmount=1.0 (the anti-cascade guarantee from the original fix is unchanged). A REASONED value, not exhaustively ear-tuned. Ear-confirmed fixed — "sounds great now."

**Freeze quick-toggle recovered, 2026-08-22, same day, then relocated.** User asked to bring back a checkbox (removed when Freeze became a dial) for quickly snapping to full freeze. Added `freezeQuickToggle` in `PluginEditor` — NOT an APVTS-attached parameter, purely a UI convenience that drives `freezeSlider.setValue()` like the pitch presets already do. Checking remembers the dial's current value then jumps it to 1.0; unchecking restores the remembered value. Deliberately does not track manual drags of the dial afterward (no listener syncing checkbox state) -- a simple one-shot jump/recall gesture, matching this phase's "functional, not final-polish" editor scope. First placed directly above the Freeze knob (window heightened 320->340 for its own reserved row); per a follow-up request, moved into the top bar to the left of `bypassButton` instead (window back to 320x320, matching the pre-dial layout's toggle-row convention).

**Freeze Amount rename, 2026-08-22, same day.** To read distinctly from the `freezeQuickToggle` checkbox above (both were previously labelled "Freeze"), the dial's display name and on-screen label were renamed to "Freeze Amount" (`Parameters.cpp`'s `AudioParameterFloat` display string and `PluginEditor`'s `freezeLabel` text) — `ParamIDs::freeze`'s underlying string ID is unchanged, so no state-compatibility break.

**Status: closed.** All Phase 9 tasks and Definition-of-Done items implemented and unit-tested (33/33 `MilleniaTests` pass); the three follow-up issues raised during ear-testing (drone kept morphing instead of holding static, full-freeze decay/oscillation, a pitch-down side effect from the anti-cascade fix) were each investigated with real measurements and fixed or mitigated to the user's explicit satisfaction ("sounds great now"). Editor UI (Freeze Amount dial, quick-toggle, presets) confirmed working as intended. Phase 10's buffer-capture-and-loop redesign remains open as a distinct, separately-valuable future capability, not a blocker for this closure.

### Phase 10 — Freeze redesign: adjustable-length loop capture (planned, not started, added 2026-08-21)

**Goal**: replace (or offer alongside — not yet decided) Phase 9's "pin decay near-unity" sustain mechanism with true buffer capture-and-loop: engaging Freeze captures a window of recent output and repeats that exact window on a hard, crossfaded loop, so what's heard is recognizably "the last sounding portion," not an ever-morphing drone. This is option (b) from Phase 9's original mechanism decision, previously not pursued because option (a) was cheaper and untested-insufficient at the time — since resolved for the drone-character complaint by the pitch-ratio crossfade fix above; this phase now stands on its own merit as a distinct loop-capture feature, not a required fix.

**New motivating case, 2026-08-29**: the `editor`'s `freezeQuickToggle` (a quick full-freeze checkbox alongside the continuous Freeze Amount dial, see `PluginEditor.cpp`) produces an audible "DJ scratch"-like pitch-bend artifact on every click, described by ear as distinctly worse than moving the dial manually. Root cause: the checkbox jumps `freezeAmount`'s smoothed target across the ENTIRE 0..1 range at once, which drives `ShimmerReverbEngine::updateShifterRatio()`'s pitch-ratio crossfade (see `docs/formant-preserving-pitch-shifter-research.md` section 14) through its full range too — an audible pitch glide, by construction, regardless of how slowly that ramp is driven. Tried and REVERTED: lengthening the smoothing ramp (0.05s -> 0.3s -> 1.0s) made it WORSE, not better, since a longer ramp only prolongs an unwanted characteristic rather than removing it — confirming this genuinely is a "wrong mechanism," not a "too fast" problem. The user's own recollection matches this diagnosis exactly: in an earlier version (before the pitch-ratio crossfade existed at all), the toggle just "kept droning" while checked and "returned to its previous state" while unchecked, with no pitch effect either way. **This is precisely what Phase 10's buffer capture-and-loop redesign would fix inherently** — crossfading between the LIVE processing chain's output and a captured/held buffer is a level/mix crossfade, not a pitch-ratio crossfade, so a toggle-triggered engage/disengage would never need to glide the shifter's own ratio at all. Full investigation trail: Engram topic_key `millenia/freeze-toggle-pitch-artifact`.

Open design questions (not yet settled — do this analysis before writing code, same discipline as every prior phase):
- **Capture source and timing**: most likely a continuously-updated rolling ring buffer of the engine's recent WET output (post-tank, pre-mix), sized to the loop-length dial's current/maximum value, so whatever the buffer holds at the instant Freeze engages becomes the loop content. Needs a decision on whether the buffer resizes live as the dial moves (real-time-safe resizing is nontrivial — likely needs a pre-allocated max-length buffer sized in `prepareToPlay`/`prepare()`, per the `juce-plugin-dev` skill's "no allocation in `processBlock`" hard rule) or the dial only takes effect on the next Freeze engagement.
- **New "Loop Length" APVTS parameter**: range/default not yet chosen (needs an ear-tuning pass once built, same as `damping`'s range was a reasoned-not-tuned starting point in Phase 5).
- **Loop-point splice**: a hard cut at the loop boundary will click/pop on every repeat — needs a crossfade at the seam (short overlap between the buffer's tail and head), sized independently of the loop length itself.
- **Interaction with Phase 9's existing mechanism**: does Phase 10 replace `setFreezeAmount()`'s decay-pin approach entirely, or add a second, separate control (e.g. a "Freeze Mode" choice, or the loop-length dial itself acting as the switch — dial at 0/minimum meaning "use the Phase 9 drone behavior")? Not decided; needs its own design pass before tasks are written.
- **Relationship to the tank's own recirculation**: should the captured loop still pass through the live tank/shifter (so it keeps some organic movement) or play back as a pure, static sample loop independent of the DSP chain from that point on? This materially changes both the implementation and the character, and wasn't resolved in this conversation.

Not scheduled to specific tasks yet — next session should start by resolving the open questions above (likely via `sdd-explore`/a short design discussion) before writing `createParameterLayout()`/buffer code, per this project's established plan-before-code discipline.

### Backlog — noted 2026-08-21, not scheduled to a phase yet

Raised during Phase 8's ear-testing; captured here so they aren't lost, not yet designed or committed to a specific phase:

- **User-controllable shimmer sustain/decay.** `maxShimmerBlendWeight` (see Phase 8) is currently a hardcoded constant. Worth asking whether it should become its own APVTS parameter (independent of `Feedback`/`shimmerAmount`) so the user can directly control how long the shimmer layer itself sustains, rather than only indirectly via the tank's overall `decayGain`.
- **A shimmer-specific ADSR/envelope.** A bigger idea than the bullet above: instead of (or in addition to) a single sustain constant, expose a proper Attack/Decay/Sustain/Release envelope specifically shaping the shimmer layer's amplitude over time (independent of the plain tail's own decay), for expressive control over how the shimmer blooms in and fades out per note/gesture. Meaningfully more DSP/UI work than a single new knob (envelope trigger logic, retrigger behavior, its own APVTS parameter set) — needs its own design pass, not a quick add.
- **Preset storage.** Distinct from Phase 9's pitch-shift quick-select buttons (which just set one existing parameter to a fixed value): a real preset system letting the user save/recall a full snapshot of all parameters (and recall it later, ideally by name). Not designed yet — open questions include storage location/format and whether to lean on `juce::AudioProcessorValueTreeState`'s own state serialization (already used for host save/reload) or a separate preset-manager abstraction. **Re-raised by the user, 2026-08-22**, suggested as Phase 10 or 11 — motivation is twofold: easier A/B testing of different setups during development, and a real end-user convenience (storing a few go-to configs) once the plugin ships. Still just noted, not designed or scheduled to a specific phase yet.
- Freeze (Phase 9, already scheduled above) and the two missing pitch presets are not re-listed here — they're already tracked, just not yet implemented.

## File/class structure proposal

```
Source/
  PluginProcessor.h/.cpp        Thin host shell: owns the APVTS, owns one ShimmerReverbEngine instance,
                                 forwards prepareToPlay/processBlock/state calls. No DSP math lives here.
  PluginEditor.h/.cpp            GUI components + APVTS attachments only.
  Parameters.h/.cpp              createParameterLayout(), parameter ID string constants, defaults/ranges.

  DSP/
    ShimmerReverbEngine.h/.cpp   Top-level DSP object: composes the diffuser, tank A/B, pitch shifter,
                                 limiter, and DC blocker into the full signal path. Exposes
                                 prepare(ProcessSpec) and process(AudioBlock<float>&); this is the
                                 only DSP class PluginProcessor talks to directly.
    DattorroTank.h/.cpp          Input diffuser (4x series hand-rolled Schroeder allpass) + two
                                 cross-feeding tank branches with damping. Owns its own
                                 DelayLine<Lagrange3rd> instances; damping is a plain one-pole
                                 leaky integrator, not an IIR::Filter. Exposes tunable points
                                 (decay gain, damping coefficient) as plain setters, not
                                 parameters itself. The sole tank in the signal path as of
                                 Phase 2 completion — Phase 1's throwaway ScratchSchroederTank
                                 has been removed.
    PitchShifter.h/.cpp          Hand-rolled dual-delay-line crossfade shifter: circular buffer,
                                 two read pointers, interpolator(s), crossfade logic. Takes a
                                 pitch-ratio (or semitone) value per block; owns no parameter
                                 knowledge itself.
    DCBlocker.h                  One-pole DC-blocking filter, used at the feedback loop boundary.
    SafetyLimiter.h/.cpp          Soft-clip/limiter safety net placed inside the feedback loop.
```

This keeps every class that participates in the feedback loop hand-written and explicit (no `ProcessorChain` anywhere under `DSP/`), matches the "PluginProcessor stays thin" convention implied by the `juce-plugin-dev` skill's parameter/state rules, and gives the test runner (`Tests/MilleniaTests.jucer`, standing since Phase 1 — see Phase 7's task list and "Risks / open decisions") a natural one-class-per-file target for unit tests (`PitchShifterTests.cpp`, etc., one per `DSP/` class, alongside the existing `DattorroTankTests.cpp`).

## Testing strategy

Mapping phases to the `juce-plugin-testing` skill's workflow:

| Phase | Unit-testable now? | Ear/pluginval validation needed? |
|---|---|---|
| 0 — Scaffolding | No DSP yet | Build/load sanity only |
| 1 — Scratch tank | Delay-line indexing at known lengths (impulse timing) | Ear: tail sounds dense/smooth, no per-block clicks |
| 2 — Dattorro tank | Delay-line indexing, allpass coefficient generation (`makeAllPass()` output at known params) | Ear: plate character, no metallic ringing; sample-rate/block-size change resilience |
| 3 — Pitch shifter | Pitch-ratio math (`2^(semitones/12)` at boundary values), read-pointer/crossfade indexing, grain-buffer sizing at −24 st | Ear: shimmer character correct direction and pitch at test values; no runaway over sustained input |
| 4 — Gain/limiter/decorrelation | DC blocker (DC in → ~0 out), limiter threshold behavior at known input levels | Ear: L/R decorrelation, no catastrophic clipping under sustained loud input |
| 5 — APVTS parameters | Parameter round-trip (`setStateInformation(getStateInformation())`) for every parameter | pluginval automation pass (parameter automation without crashes/glitches) |
| 6 — Editor | N/A (GUI, not DSP math) | Manual: every control drives the matching parameter; no processor state touched directly |
| 7 — Testing/validation | All of the above consolidated into one test runner target, run across block sizes 1/7/512/4096 and sample rates 44100/48000/96000 | `pluginval --strictness-level 10` against the built VST3; final ear pass |
| 8 — Shimmer character alignment | `shimmerAmount` state round-trip; bounded/finite output at `shimmerAmount`/`Feedback` extremes; regression test that default `shimmerAmount=1.0f` output is unchanged from pre-Phase-8 | Ear: shimmer reads as a distinct ascending pitched wash, not "a slightly different reverb"; ideally A/B'd against a reference shimmer plugin |
| 9 — Freeze / drone capture, presets | Freeze parameter state round-trip; bounded output over several minutes with Freeze engaged (near-unity `decayGain` is a genuinely new stability case, not covered by Phase 4's existing tests) | Ear: no click at Freeze engage/disengage; all 7 pitch-shift presets set the expected value |

General rule from the skill (applied throughout, not just Phase 7): test DSP correctness (output values, state, indexing) — never assert on wall-clock timing behavior, that's flaky by construction.

## Risks / open decisions

These are genuinely unresolved by the three source docs; flagging them honestly rather than inventing false certainty:

- ~~**`juce_dsp` module is currently absent from `Millenia.jucer`.**~~ **Resolved in Phase 0.** Module + VS2026 `<MODULEPATH>` added, confirmed by a clean build.
- ~~**Plugin formats and channel configuration are not explicit in the `.jucer` file.**~~ **Resolved in Phase 0** (by review, not by editing): `pluginFormats="buildVST3,buildAU,buildStandalone"` — VST3+Standalone are what the VS2026 exporter actually produces; `buildAU` is harmless/inert without an Xcode exporter and was deliberately left rather than stripped, in case a macOS exporter is added later. Channel config is the JUCE template's stereo-in/stereo-out `BusesProperties`, confirmed as the intended config for a reverb effect (not touched).
- **Exact Dattorro delay-line lengths.** Both source docs are explicit that Dattorro's (1997) and Freeverb's published values are a *starting point, not a literal port*. The actual shipped lengths need empirical tuning during Phase 2 against this project's target decay character — no one has picked final numbers yet.
- **Exact safety limiter design.** The docs specify "a soft-clip/limiter safety net," not a specific algorithm (simple `tanh`/`std::clamp` soft-clip vs. a proper lookahead peak limiter vs. something else). This needs a decision during Phase 4, and it affects both CPU cost and how audibly it announces itself when it engages.
- **Decorrelation method.** `shimmer-reverb-concepts.md` explicitly presents this as an "or": different delay lengths/modulation per channel, *or* quadrature-offset pitch shifting (Airwindows `Galactic`-style). The docs don't pick one — Phase 4 needs a deliberate choice, ideally informed by a quick A/B rather than defaulting to whichever is easier to code.
- **Whether/when to add oversampling around the pitch shifter.** Both docs agree it's "not a day-one requirement" and should be added "only if aliasing is still audible" after Lagrange3rd interpolation — this is a deferred, conditional decision, not a scheduled task, and Phase 3's definition of done doesn't currently require it.
- ~~**No test runner target exists yet.**~~ **Resolved, pulled forward from Phase 7.** A separate Projucer `consoleapp` project, `Tests/MilleniaTests.jucer`, references `Source/DSP/*.cpp` by relative path (not copies) and builds its own `Tests/Builds/VisualStudio2026/MilleniaTests.sln`. `Tests/Source/Main.cpp` runs a `juce::UnitTestRunner` subclass that logs to stdout and returns a non-zero exit code on any failure — this is what future `juce::UnitTest` subclasses (per-`DSP/` class, as sketched in "File/class structure proposal" below) plug into as each real class lands, starting now instead of waiting for Phase 7. Module closure: `juce_core`, `juce_audio_basics`, `juce_audio_formats`, `juce_dsp` (the full dependency chain `juce_dsp` actually needs — verified against JUCE's own module declarations, not guessed).
- **Double-precision processing support.** The `juce-plugin-dev` skill requires a *deliberate* decision on `supportsDoublePrecisionProcessing()`/`getProcessingPrecision()`, not a silent float-only truncation. None of the shimmer docs address this for the Dattorro tank or pitch shifter specifically (e.g. whether `DelayLine<double, Lagrange3rd>` instantiations are needed). Needs an explicit choice, likely around Phase 2–3.
- **Feedback gain's final default value.** The architecture doc gives a range (0.6–0.85) and says "tune by ear," not a committed single default — Phase 4/5 needs to land on one specific number (and possibly per-preset values) rather than treating the range itself as the answer.
- ~~**Freeze's mechanism and drone length (Phase 9).**~~ **Resolved 2026-08-16.** Chose option (a), the classic near-unity-`decayGain` infinite-sustain trick (mute dry input, pin `decayGain` near unity) over true buffer capture-and-loop — user's call: "let's try option a first, I think it will be enough." This also removes the drone-length question entirely (no fixed capture window under this mechanism); buffer capture-and-loop remains documented in Phase 9 as a fallback if (a) proves insufficient by ear, not pursued now.

## References

- [`shimmer-reverb-concepts.md`](./shimmer-reverb-concepts.md)
- [`../.claude/skills/juce-plugin-dev/references/shimmer-reverb-architecture.md`](../.claude/skills/juce-plugin-dev/references/shimmer-reverb-architecture.md)
- [`shimmer-reverb-open-source-survey.md`](./shimmer-reverb-open-source-survey.md)
- [`reverb-design-research-notes.md`](./reverb-design-research-notes.md)
- [`reverb-dev-journal.md`](./reverb-dev-journal.md) — running log of what's actually been built against this plan, phase by phase
